<?xml version="1.0" encoding="UTF-8"?><rss xmlns:dc="http://purl.org/dc/elements/1.1/" xmlns:content="http://purl.org/rss/1.0/modules/content/" xmlns:atom="http://www.w3.org/2005/Atom" version="2.0">
    <channel>
        <title><![CDATA[Bougie's Blog]]></title>
        <description><![CDATA[Bougie's Blog, Bougie 的博客]]></description>
        <link>https://bougieL.github.io</link>
        <generator>RSS for Node</generator>
        <lastBuildDate>Sat, 26 Sep 2026 19:04:52 GMT</lastBuildDate>
        <atom:link href="https://bougieL.github.io/rss.xml" rel="self" type="application/rss+xml"/>
        <copyright><![CDATA[Bougie 2026]]></copyright>
        <language><![CDATA[zh-CN]]></language>
        <item>
            <title><![CDATA[网页还能更快吗：Speculation Rules、bfcache 和"感觉没加载"这件事]]></title>
            <description><![CDATA[我把博客的 LCP 从两秒多压到一秒内，靠的不是再一次拆包，而是让浏览器在用户还没点击的时候就把下一页准备好。这篇记录一下 Speculation Rules 的写法与取舍、bfcache 为什么老是不生效、以及为什么"快"到最后是个感知问题而不是数字问题。]]></description>
            <link>https://bougieL.github.io/2026/09/27/speculation-rules-instant-navigation/</link>
            <guid isPermaLink="true">https://bougieL.github.io/2026/09/27/speculation-rules-instant-navigation/</guid>
            <pubDate>Sun, 27 Sep 2026 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<p>有段时间我一直在跟博客的首屏数字较劲。压缩、拆包、图片转 AVIF、字体子集化，能做的都做了，LCP 停在两秒出头，再也下不去。</p> <p>后来我换了个思路：<strong>别让它更快，让它根本不用加载。</strong></p> <p><img src="https://bougieL.github.io/images/2026/09/27/cover_001.jpg" alt="深色桌面上放着一台笔记本，屏幕里的网页瞬间出现，四周有速度光线和发光的网络连线汇聚，深蓝青色调" loading="lazy"></p> <p>点开文章页的那一刻，页面已经在后台渲染好了，切换是零延迟的。这不是什么黑科技，是浏览器这几年悄悄补齐的一组能力：预取、预渲染、bfcache、以及配套的 View Transitions。这篇把它们串起来讲一遍，顺便记录我踩过的几个坑。</p> <h2 id="一、导航为什么慢-它默认是串行的"> 一、导航为什么慢：它默认是串行的</h2> <p>一次普通的页面跳转，时间大致花在这些事情上，而且是<strong>排队发生</strong>的：</p> <ol><li>用户点击 → 浏览器发请求</li> <li>服务器响应 HTML（一个 RTT 起步）</li> <li>解析 HTML → 发现 CSS、JS、字体 → 再发请求（又一个 RTT）</li> <li>CSSOM 就绪 → 布局 → 绘制 → 首屏</li></ol> <p>前面这些优化（preload、Early Hints、内联关键 CSS）本质上都是在<strong>压缩第 3 步和第 2 步之间的等待</strong>。但它们没法消灭"点击之后才开始干活"这件事——从点击到第一个字节回来之间，至少有一个 RTT 是纯浪费的，在移动网络上这可能是几百毫秒。</p> <p>而 SPA 之所以给人"快"的错觉，是因为路由切换时它跳过了第 2 步：HTML 早就在手里了，只取数据。代价是你得维持一整套客户端路由、数据层和状态管理。</p> <p><strong>浏览器原生给出的答案是：把第 2、3、4 步提前到用户点击之前。</strong></p> <p><img src="https://bougieL.github.io/images/2026/09/27/fig1_001.jpg" alt="浏览器窗口上方悬浮着两幅全息对比图：左边是资源文件被悄悄下载进缓存盒，右边是完整渲染好的页面等在半透明幕布后面，蓝色科技风" loading="lazy"></p> <h2 id="二、老办法都挺别扭"> 二、老办法都挺别扭</h2> <p>这条路其实走过一轮，而且留下了不少废墟：</p> <ul><li><code>&lt;link rel="prefetch"&gt;</code>：只把资源抓进 HTTP 缓存，<strong>不解析不执行</strong>。对 JS、图片有效，对"下一页的 HTML"有效性一般，而且没有任何"何时抓"的策略，写上去就抓。</li> <li><code>&lt;link rel="prerender"&gt;</code>：Chrome 很早就支持，但因为各家实现不一致、且没有节制机制，被两次废弃，最后彻底让位给新的 API。现在写它等于没写。</li> <li><strong>Quicklink / Guess.js</strong>：这两类库做的事情是"视口内 / 大概率被点的链接，用 prefetch 抓一下"。思路是对的，但它们只能 prefetch（抓资源），做不到真正的预渲染，而且规则写在 JS 里，浏览器看不见意图，也就没法做资源调度。</li></ul> <p>所以 2026 年再看，正确的入口是 <strong>Speculation Rules API</strong>——它把"猜用户会点哪"这件事变成了一份浏览器能读懂的声明。</p> <h2 id="三、speculation-rules-把猜测写成声明"> 三、Speculation Rules：把猜测写成声明</h2> <p>用法就是一段 JSON，放在 <code>&lt;script type="speculationrules"&gt;</code> 里：</p> <div class="language-html extra-class"><pre class="language-html"><code><span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>script</span> <span class="token attr-name">type</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">"</span>speculationrules<span class="token punctuation">"</span></span><span class="token punctuation">&gt;</span></span><span class="token script"><span class="token language-javascript">
  <span class="token punctuation">{</span>
    <span class="token string-property property">"prerender"</span><span class="token operator">:</span> <span class="token punctuation">[</span>
      <span class="token punctuation">{</span>
        <span class="token string-property property">"where"</span><span class="token operator">:</span> <span class="token punctuation">{</span>
          <span class="token string-property property">"href_matches"</span><span class="token operator">:</span> <span class="token string">"/blog/*"</span>
        <span class="token punctuation">}</span><span class="token punctuation">,</span>
        <span class="token string-property property">"eagerness"</span><span class="token operator">:</span> <span class="token string">"moderate"</span>
      <span class="token punctuation">}</span>
    <span class="token punctuation">]</span><span class="token punctuation">,</span>
    <span class="token string-property property">"prefetch"</span><span class="token operator">:</span> <span class="token punctuation">[</span>
      <span class="token punctuation">{</span>
        <span class="token string-property property">"where"</span><span class="token operator">:</span> <span class="token punctuation">{</span>
          <span class="token string-property property">"selector_matches"</span><span class="token operator">:</span> <span class="token string">".card a"</span>
        <span class="token punctuation">}</span><span class="token punctuation">,</span>
        <span class="token string-property property">"eagerness"</span><span class="token operator">:</span> <span class="token string">"conservative"</span>
      <span class="token punctuation">}</span>
    <span class="token punctuation">]</span>
  <span class="token punctuation">}</span>
</span></span><span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>script</span><span class="token punctuation">&gt;</span></span>
</code></pre></div><p>它有两档动作：</p> <ul><li><strong><code>prefetch</code></strong>：把目标文档抓下来放进一个特殊缓存，<strong>不执行 JS、不渲染</strong>。成本低，安全，但只能省掉网络时间。</li> <li><strong><code>prerender</code></strong>：在后台开一个隐藏页面，完整加载、渲染、执行 JS。用户点下去时是<strong>直接切过去</strong>，连布局都做完了。成本高，收益也最大。</li></ul> <h3 id="两种写法"> 两种写法</h3> <div class="language-json extra-class"><pre class="language-json"><code><span class="token comment">// 1) 直接列 URL（list 规则）</span>
<span class="token punctuation">{</span> <span class="token property">"prerender"</span><span class="token operator">:</span> <span class="token punctuation">[</span><span class="token punctuation">{</span> <span class="token property">"source"</span><span class="token operator">:</span> <span class="token string">"list"</span><span class="token punctuation">,</span> <span class="token property">"urls"</span><span class="token operator">:</span> <span class="token punctuation">[</span><span class="token string">"/a"</span><span class="token punctuation">,</span> <span class="token string">"/b"</span><span class="token punctuation">]</span><span class="token punctuation">,</span> <span class="token property">"eagerness"</span><span class="token operator">:</span> <span class="token string">"immediate"</span> <span class="token punctuation">}</span><span class="token punctuation">]</span> <span class="token punctuation">}</span>

<span class="token comment">// 2) 按条件匹配页面里的链接（document 规则）</span>
<span class="token punctuation">{</span> <span class="token property">"prefetch"</span><span class="token operator">:</span> <span class="token punctuation">[</span><span class="token punctuation">{</span> <span class="token property">"source"</span><span class="token operator">:</span> <span class="token string">"document"</span><span class="token punctuation">,</span>
                <span class="token property">"where"</span><span class="token operator">:</span> <span class="token punctuation">{</span> <span class="token property">"and"</span><span class="token operator">:</span> <span class="token punctuation">[</span>
                  <span class="token punctuation">{</span> <span class="token property">"href_matches"</span><span class="token operator">:</span> <span class="token string">"/blog/*"</span> <span class="token punctuation">}</span><span class="token punctuation">,</span>
                  <span class="token punctuation">{</span> <span class="token property">"not"</span><span class="token operator">:</span> <span class="token punctuation">{</span> <span class="token property">"selector_matches"</span><span class="token operator">:</span> <span class="token string">".no-prefetch"</span> <span class="token punctuation">}</span> <span class="token punctuation">}</span>
                <span class="token punctuation">]</span><span class="token punctuation">}</span><span class="token punctuation">,</span>
                <span class="token property">"eagerness"</span><span class="token operator">:</span> <span class="token string">"moderate"</span> <span class="token punctuation">}</span><span class="token punctuation">]</span> <span class="token punctuation">}</span>
</code></pre></div><p><code>where</code> 里可以用 <code>href_matches</code>（URL 模式，支持 <code>*</code> 通配）、<code>selector_matches</code> / <code>selector_exclude</code>（CSS 选择器）、<code>and</code> / <code>or</code> / <code>not</code>。我自己的用法是<strong>只对文章详情页用 prerender，对列表页只 prefetch</strong>——因为列表页可能很重，而详情页是用户最终的目标。</p> <h3 id="eagerness-是这套-api-的节流阀"> eagerness 是这套 API 的节流阀</h3> <p>这是个关键参数，四个档位：</p> <table><thead><tr><th>值</th> <th>触发时机</th> <th>默认用于</th></tr></thead> <tbody><tr><td><code>immediate</code></td> <td>规则一解析完就动手</td> <td>URL list 规则</td></tr> <tr><td><code>eager</code></td> <td>目前与 immediate 行为一致（留作将来更激进的调度）</td> <td>—</td></tr> <tr><td><code>moderate</code></td> <td>鼠标悬停约 200ms，或 mousedown/touchstart 立即触发</td> <td>—</td></tr> <tr><td><code>conservative</code></td> <td>只在 mousedown / touchstart（也就是真的按下去了）</td> <td>document 规则</td></tr></tbody></table> <p><strong>默认是保守的</strong>，这点设计得很好——它意味着你随便写一份规则也不会把用户的流量烧光。我一般给详情页 <code>moderate</code>：悬停 200ms 才动手，过滤掉了大部分"鼠标扫过去"的误判。</p> <p><img src="https://bougieL.github.io/images/2026/09/27/fig2_001.jpg" alt="发光的 JSON 规则表悬浮在浏览器前方，光束指向若干条被提前准备的候选链接，深蓝赛博风格插画" loading="lazy"></p> <h3 id="跨站和参数-两个要注意的点"> 跨站和参数：两个要注意的点</h3> <p><strong>跨站预渲染需要对方点头。</strong> 同站（same-site）规则可以直接用；跨站 prerender 要目标站点返回 <code>Supports-Loading-Mode: credentialed-prerender</code>，否则浏览器会降级甚至放弃。这条是对隐私的硬保护——你不能替用户去别的站点"假装访问"。</p> <p><strong>查询参数会让预渲染白干。</strong> 如果链接是 <code>/post?id=1&amp;from=list</code>，而用户点的是 <code>/post?id=1&amp;from=list&amp;t=1234</code>，那后台渲染好的那一份对不上，预渲染作废。解决方式是服务端（或 <code>&lt;meta&gt;</code>）声明：</p> <div class="language- extra-class"><pre class="language-text"><code>No-Vary-Search: params=("t"), except=("id")
</code></pre></div><p>配合规则里的 <code>"expects_no_vary_search": true</code>，告诉浏览器"这些参数不影响页面内容，可以对上就用"。</p> <h2 id="四、别让预渲染把你的数据搞脏"> 四、别让预渲染把你的数据搞脏</h2> <p>这是我最想强调的一条，也是最容易出事的。</p> <p><strong>预渲染是真的把页面跑了一遍。</strong> JS 执行了，图片请求发出去了，埋点也发出去了。如果你的统计代码不加区分，会出现三种难看的后果：</p> <ol><li>页面浏览量虚高（预渲染了一次没被点击）</li> <li>转化漏斗对不上（点了才算的核心事件被提前上报）</li> <li>后台出现"幽灵请求"，消耗带宽和配额</li></ol> <p>防护手段，客户端和服务端各一套：</p> <div class="language-js extra-class"><pre class="language-js"><code><span class="token comment">// 客户端：预渲染阶段先别上报</span>
<span class="token keyword">if</span> <span class="token punctuation">(</span>document<span class="token punctuation">.</span>prerendering<span class="token punctuation">)</span> <span class="token punctuation">{</span>
  document<span class="token punctuation">.</span><span class="token function">addEventListener</span><span class="token punctuation">(</span><span class="token string">'prerenderingchange'</span><span class="token punctuation">,</span> <span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">=&gt;</span> <span class="token punctuation">{</span>
    <span class="token comment">// 页面被真正激活后才开始</span>
    <span class="token function">reportPageView</span><span class="token punctuation">(</span><span class="token punctuation">)</span>
  <span class="token punctuation">}</span><span class="token punctuation">,</span> <span class="token punctuation">{</span> <span class="token literal-property property">once</span><span class="token operator">:</span> <span class="token boolean">true</span> <span class="token punctuation">}</span><span class="token punctuation">)</span>
<span class="token punctuation">}</span> <span class="token keyword">else</span> <span class="token punctuation">{</span>
  <span class="token function">reportPageView</span><span class="token punctuation">(</span><span class="token punctuation">)</span>
<span class="token punctuation">}</span>
</code></pre></div><div class="language-js extra-class"><pre class="language-js"><code><span class="token comment">// 事后判断是否来自预渲染</span>
<span class="token keyword">const</span> nav <span class="token operator">=</span> performance<span class="token punctuation">.</span><span class="token function">getEntriesByType</span><span class="token punctuation">(</span><span class="token string">'navigation'</span><span class="token punctuation">)</span><span class="token punctuation">[</span><span class="token number">0</span><span class="token punctuation">]</span>
<span class="token keyword">if</span> <span class="token punctuation">(</span>nav<span class="token punctuation">.</span>activationStart <span class="token operator">&gt;</span> <span class="token number">0</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
  <span class="token comment">// 这次导航命中了 prerender，activationStart 是激活时刻</span>
<span class="token punctuation">}</span>
</code></pre></div><p>服务端这边，Chrome 会在预取请求上带一个 <code>Sec-Purpose</code> 头：</p> <ul><li>prefetch：<code>Sec-Purpose: prefetch</code></li> <li>prerender：<code>Sec-Purpose: prefetch;prerender</code></li> <li>需要匿名 IP 的跨站场景：<code>Sec-Purpose: prefetch;anonymous-client-ip</code></li></ul> <p>看到这个头，你可以选择照常响应，也可以直接回 <code>204</code> / <code>503</code> 拒绝它。<strong>至少应该在日志里把它标出来</strong>，否则你永远不知道那些多出来的请求是哪来的。</p> <blockquote><p>反过来也成立：不要在预渲染页面里做任何"有副作用"的事——发送站内消息、扣减额度、写数据库。这些应该等到 <code>prerenderingchange</code> 之后。</p></blockquote> <h2 id="五、bfcache-后退键本来应该是零成本的"> 五、bfcache：后退键本来应该是零成本的</h2> <p>讲完"往前走"，再说"往回走"。</p> <p>浏览器有个 <strong>bfcache（Back/Forward cache）</strong>：离开页面时不销毁它，而是把整个页面（包括 JS 堆、DOM、滚动位置、未完成的定时器）冻结下来放进内存。用户按后退时直接解冻，<strong>没有任何网络请求，也没有重新执行 JS</strong>。</p> <p>理论上后退应该是瞬间的。实际上它经常不生效，而且你很难察觉——只是"感觉后退有点卡"。</p> <p><img src="https://bougieL.github.io/images/2026/09/27/fig3_001.jpg" alt="巨大的浏览器后退箭头，箭头里有一个被保存在玻璃罩中的网页快照，冷青色调光线，现代极简插画" loading="lazy"></p> <p>我查过自己博客为什么没命中，原因很典型：</p> <div class="language-js extra-class"><pre class="language-js"><code><span class="token comment">// 1) unload 监听器：最经典的杀手</span>
window<span class="token punctuation">.</span><span class="token function">addEventListener</span><span class="token punctuation">(</span><span class="token string">'unload'</span><span class="token punctuation">,</span> <span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">=&gt;</span> <span class="token punctuation">{</span> <span class="token comment">/* 上报退出 */</span> <span class="token punctuation">}</span><span class="token punctuation">)</span>
<span class="token comment">// → 浏览器无法安全地冻结一个有 unload 处理的页面，直接放弃 bfcache</span>
</code></pre></div><p>常见的阻断项：</p> <table><thead><tr><th>阻断原因</th> <th>说明</th></tr></thead> <tbody><tr><td><code>unload</code> 监听器</td> <td>头号杀手，用 <code>pagehide</code> / <code>visibilitychange</code> 替代</td></tr> <tr><td>主文档 <code>Cache-Control: no-store</code></td> <td>常见于带登录态的页面</td></tr> <tr><td>未关闭的连接</td> <td>IndexedDB 事务、WebSocket、WebRTC 连接仍在活动</td></tr> <tr><td>打开的子窗口 / <code>window.opener</code> 引用</td> <td>引用链还在就无法冻结</td></tr> <tr><td>页面用了 <code>SharedWorker</code></td> <td>需要特殊处理</td></tr></tbody></table> <p>怎么查：</p> <div class="language-js extra-class"><pre class="language-js"><code><span class="token comment">// 页面被从 bfcache 恢复时会触发，persisted 为 true</span>
window<span class="token punctuation">.</span><span class="token function">addEventListener</span><span class="token punctuation">(</span><span class="token string">'pageshow'</span><span class="token punctuation">,</span> <span class="token punctuation">(</span><span class="token parameter">e</span><span class="token punctuation">)</span> <span class="token operator">=&gt;</span> <span class="token punctuation">{</span>
  <span class="token keyword">if</span> <span class="token punctuation">(</span>e<span class="token punctuation">.</span>persisted<span class="token punctuation">)</span> console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span><span class="token string">'从 bfcache 恢复'</span><span class="token punctuation">)</span>
<span class="token punctuation">}</span><span class="token punctuation">)</span>

<span class="token comment">// 想知道为什么没被缓存</span>
<span class="token keyword">const</span> nav <span class="token operator">=</span> performance<span class="token punctuation">.</span><span class="token function">getEntriesByType</span><span class="token punctuation">(</span><span class="token string">'navigation'</span><span class="token punctuation">)</span><span class="token punctuation">[</span><span class="token number">0</span><span class="token punctuation">]</span>
console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span>nav<span class="token punctuation">.</span>notRestoredReasons<span class="token punctuation">)</span>
<span class="token comment">// blocked / reasons 里会写明具体原因</span>
</code></pre></div><p>Chrome DevTools 也有个 <strong>Application → Back/forward cache</strong> 面板，直接点一下就能跑测试并给出原因。</p> <p>我把那个 <code>unload</code> 上报换成 <code>pagehide</code> 之后，后退终于变成零延迟了。<strong>这个改动五分钟，收益比调一周打包配置还明显。</strong></p> <h2 id="六、-快-到最后是个感知问题"> 六、"快"到最后是个感知问题</h2> <p>做到这里，你会发现一个有意思的现象：bfcache 和 prerender 都把加载时间降到了接近零，但<strong>体验上的提升并不等于数字上的提升</strong>。因为人对"快"的感受，很大一部分来自连续性。</p> <p>如果页面内容瞬间就位，但因为整屏白闪了一下，用户依然会觉得"卡了一下"。</p> <p>这就是 <strong>View Transitions API</strong> 要补的另一半。同一文档内：</p> <div class="language-js extra-class"><pre class="language-js"><code>document<span class="token punctuation">.</span><span class="token function">startViewTransition</span><span class="token punctuation">(</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">=&gt;</span> <span class="token punctuation">{</span>
  <span class="token comment">// 在这里更新 DOM，浏览器会自动截图并做过渡</span>
  <span class="token function">renderNextPage</span><span class="token punctuation">(</span><span class="token punctuation">)</span>
<span class="token punctuation">}</span><span class="token punctuation">)</span>
</code></pre></div><p>跨文档（也就是真正的 MPA 跳转）更简单，两端页面的 CSS 里都写：</p> <div class="language-css extra-class"><pre class="language-css"><code><span class="token atrule"><span class="token rule">@view-transition</span></span> <span class="token punctuation">{</span>
  <span class="token property">navigation</span><span class="token punctuation">:</span> auto<span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre></div><p>然后给需要"连续"的元素起名字：</p> <div class="language-css extra-class"><pre class="language-css"><code><span class="token selector">.article-title</span> <span class="token punctuation">{</span> <span class="token property">view-transition-name</span><span class="token punctuation">:</span> article-title<span class="token punctuation">;</span> <span class="token punctuation">}</span>
</code></pre></div><p>浏览器会把旧页面和新页面里同名元素配对，自动做位移、缩放和淡入淡出的插值。列表页标题变成详情页标题、卡片展开成整篇文章，这类效果是<strong>免费</strong>拿到的。</p> <p><img src="https://bougieL.github.io/images/2026/09/27/fig4_001.jpg" alt="两个不同网页布局之间的平滑变形过渡，卡片和图片渐变移动到新位置，柔和金色光轨，现代 UI 动效插画" loading="lazy"></p> <p>底层的动画可以用伪元素单独控制：</p> <div class="language-css extra-class"><pre class="language-css"><code><span class="token selector">::view-transition-old(article-title)</span> <span class="token punctuation">{</span> <span class="token property">animation-duration</span><span class="token punctuation">:</span> 200ms<span class="token punctuation">;</span> <span class="token punctuation">}</span>
<span class="token selector">::view-transition-new(article-title)</span> <span class="token punctuation">{</span> <span class="token property">animation-duration</span><span class="token punctuation">:</span> 200ms<span class="token punctuation">;</span> <span class="token punctuation">}</span>

<span class="token atrule"><span class="token rule">@media</span> <span class="token punctuation">(</span><span class="token property">prefers-reduced-motion</span><span class="token punctuation">:</span> reduce<span class="token punctuation">)</span></span> <span class="token punctuation">{</span>
  <span class="token selector">::view-transition-group(*)</span> <span class="token punctuation">{</span> <span class="token property">animation</span><span class="token punctuation">:</span> none <span class="token important">!important</span><span class="token punctuation">;</span> <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre></div><p>注意最后那段 —— <strong>转场动画一定要尊重 <code>prefers-reduced-motion</code></strong>，否则你会把一部分用户直接劝退。</p> <h2 id="七、一份可以照着做的清单"> 七、一份可以照着做的清单</h2> <p>按"投入产出比"排序，我自己的落地顺序是这样的：</p> <ol><li><strong>先删掉所有 <code>unload</code> 监听</strong>，换 <code>pagehide</code>。成本最低，立刻收获 bfcache。</li> <li><strong>用 <code>notRestoredReasons</code> 或 DevTools 面板测一遍后退</strong>，把剩下的阻断项清掉（多半是没关的 IndexedDB 连接）。</li> <li><strong>给主要目标页加 <code>prefetch</code> 规则</strong>，<code>eagerness: "conservative"</code>。几乎零风险。</li> <li><strong>确认你的埋点能识别 <code>document.prerendering</code> 和 <code>Sec-Purpose</code></strong>，再上 <code>prerender</code>，否则数据会先坏掉。</li> <li><strong>给详情页加 <code>@view-transition</code> + <code>view-transition-name</code></strong>，把"零加载"升级成"零打断"。</li> <li>用 <code>nav.activationStart &gt; 0</code> 做监控，看预渲染的命中率到底有多少——<strong>命中率低于 20% 说明你的猜测规则不准，别硬开</strong>。</li></ol> <p>关于生效范围也要说实话：prefetch / prerender 目前主要是 Chromium 系（Chrome、Edge 及同源内核）支持，Firefox 与 Safari 的跟进程度不一致，不支持的浏览器会静默忽略这段脚本，页面照常工作——<strong>这是渐进增强，不是依赖</strong>。上之前最好实测一遍。</p> <h2 id="八、一点感想"> 八、一点感想</h2> <p>我一开始抗拒这类 API，理由是"这不就是 SPA 换了个壳吗"。</p> <p>后来想明白了区别在哪：SPA 是用 JS 在<strong>应用层</strong>重新实现了一遍导航，你得自己管路由、管数据、管滚动恢复、管转场；而 Speculation Rules + bfcache + View Transitions 是浏览器在<strong>平台层</strong>把这件事做了，你只写一份 JSON 和几行 CSS，剩下的调度、节制、内存管理都由浏览器负责——包括"用户流量不够时自动不做"这种你自己很难写对的策略。</p> <p>Web 平台花了十年时间在补一个本该早就有的能力：<strong>让多页应用也能有原生应用的那种顺滑，而不需要付出整套 SPA 的复杂度。</strong> 我这个博客是纯静态 MPA，接上这些东西之后，点文章的体验已经接近本地应用了——而我的代码里没有一行客户端路由。</p> <p>这大概就是平台进步的模样：不是给你新玩具，是让你把旧包袱扔掉。</p> <hr> <p>（文中各 API 的浏览器支持情况与字段细节截至本文写作时，实际使用前请以当前版本的实测结果为准。）</p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[时间的老账：Date 三十年，和 Temporal 能不能还清]]></title>
            <description><![CDATA[我写过无数次 new Date()，也无数次被它坑过：月份从 0 开始、对象可变、时区只能靠偏移量硬凑、夏令时那天一天不是 24 小时。2026 年 3 月 Temporal 终于进了 Stage 4，成为 ES2026 的一部分。这篇记录一下 Date 的问题到底出在哪、Temporal 用什么结构去还这笔账，以及现在这个项目里到底能不能直接用。]]></description>
            <link>https://bougieL.github.io/2026/09/26/temporal-api-javascript-time/</link>
            <guid isPermaLink="true">https://bougieL.github.io/2026/09/26/temporal-api-javascript-time/</guid>
            <pubDate>Sat, 26 Sep 2026 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<p>前阵子线上出了个很典型的 bug：运营配了一个活动，"9 月 30 日 23:59 结束"，页面上倒计时却比预期早了一个小时。排查了半天，最后发现原因特别朴素——有人在配置里写了本地时间，有人写了 UTC，还有一段代码用 <code>+86400000</code> 来"加一天"。</p> <p>这三种写法在一年里的三百多天都是对的。只有夏令时切换的那两天会错。</p> <p><img src="https://bougieL.github.io/images/2026/09/26/cover_001.jpg" alt="暗色桌面上散落着几个指向不同时刻的指针时钟，空中漂浮着日历页和时区标签，暖色灯光" loading="lazy"></p> <p>这类 bug 我一直归咎于自己不够小心。但 Temporal 提案文档里有一句话让我改了想法：<strong>Date 不是被用错了，它是设计错了。</strong> 它从 1995 年被复制出来那天起，就带着一套不成立的模型。</p> <h2 id="一、date-的出身问题"> 一、Date 的出身问题</h2> <p>1995 年，Brendan Eich 用十天写出了 JavaScript 的第一版。日期部分没什么时间设计，直接照抄了当时 Java 的 <code>java.util.Date</code>——连 <code>getYear()</code> 返回 1900 偏移、月份从 0 开始这些毛病都一起抄了过来。后来 Java 自己在 1.1 版本把 <code>java.util.Date</code> 的大部分方法标记为废弃，换上了 <code>Calendar</code>，再后来 Java 8 又换成了 <code>java.time</code>。</p> <p><strong>Java 换了三代，JavaScript 这一抄，抄了三十年。</strong></p> <p><img src="https://bougieL.github.io/images/2026/09/26/fig1_001.jpg" alt="复古 CRT 显示器上显示着出错的日历界面，周围散落着写满日期又被划掉的便签，暖黄灯光" loading="lazy"></p> <p>具体的问题我按"坑到我过"的程度排个序：</p> <p><strong>1. 它是可变的。</strong> <code>const a = new Date(); const b = a; b.setMonth(b.getMonth() + 1);</code> —— <code>a</code> 跟着变了。<code>const</code> 挡不住这个，因为它保护的是引用。任何把 Date 当参数传进函数的地方，都得赌对方不会改它。</p> <p><strong>2. 月份从 0 开始，年份从 1900 开始。</strong> <code>new Date(2026, 8, 26)</code> 是 9 月 26 日。这个我到现在写的时候还要停顿半秒。</p> <p><strong>3. 它只有一个"绝对时刻"模型。</strong> Date 内部就是一个毫秒数，里面没有任何时区信息。所谓"本地时间"是读取的那一刻用运行环境时区现算出来的。这意味着同一个 Date 对象，在北京和纽约打印出来不一样，而且<strong>你没法让一个 Date 对象"固定"在东京时区</strong>。</p> <p><strong>4. 字符串解析是玄学。</strong> <code>new Date('2026-09-26')</code> 按 UTC 解析，<code>new Date('2026/09/26')</code> 按本地解析，<code>new Date('2026-09-26T00:00')</code>（没有时区后缀）在 ES2016 之后又变成按本地解析。规范里写明了：不在 ISO 8601 格式内的字符串，解析行为由实现自己决定。</p> <p><strong>5. 算术在夏令时那天是错的。</strong> 这是最要命的一条，也是开头那个 bug 的根源：</p> <div class="language-js extra-class"><pre class="language-js"><code><span class="token comment">// 美国 2026-03-08 凌晨 2 点跳到 3 点，这一天只有 23 小时</span>
<span class="token keyword">const</span> d <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">Date</span><span class="token punctuation">(</span><span class="token string">'2026-03-07T12:00:00-05:00'</span><span class="token punctuation">)</span>
d<span class="token punctuation">.</span><span class="token function">setTime</span><span class="token punctuation">(</span>d<span class="token punctuation">.</span><span class="token function">getTime</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">+</span> <span class="token number">24</span> <span class="token operator">*</span> <span class="token number">60</span> <span class="token operator">*</span> <span class="token number">60</span> <span class="token operator">*</span> <span class="token number">1000</span><span class="token punctuation">)</span>
<span class="token comment">// → 2026-03-08T13:00:00-04:00，墙上时间变成了下午 1 点，不是中午 12 点</span>

d<span class="token punctuation">.</span><span class="token function">setDate</span><span class="token punctuation">(</span>d<span class="token punctuation">.</span><span class="token function">getDate</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">+</span> <span class="token number">1</span><span class="token punctuation">)</span>
<span class="token comment">// → 2026-03-08T12:00:00-04:00，这个才是对的</span>
</code></pre></div><p><code>setTime(+24h)</code> 和 <code>setDate(+1)</code> 平时等价，切换日那天不等价。而这两行代码在代码库里长得一样无辜。</p> <h2 id="二、temporal-的核心思路-先分清楚你在说哪种时间"> 二、Temporal 的核心思路：先分清楚你在说哪种时间</h2> <p>Temporal 最漂亮的地方不是 API 更好用，而是它<strong>逼你先回答一个问题</strong>：你手里这个值，是"人类钟表上的读数"，还是"时间轴上的一个点"?</p> <p>这两件事在 Date 里被混成了一个东西，在 Temporal 里是两套类型：</p> <ul><li><strong><code>Instant</code></strong> —— 时间轴上的一个点，精确到纳秒，没有时区、没有日历，等价于"绝对时刻"。对应旧世界里的 <code>Date.getTime()</code>。</li> <li><strong><code>PlainDate</code> / <code>PlainTime</code> / <code>PlainDateTime</code></strong> —— 墙上时间，就是日历上写的那个"2026 年 9 月 26 日 20:00"，不带任何时区。它<strong>不需要</strong>时区，也不允许你问它"这是 UTC 几点"。</li> <li><strong><code>ZonedDateTime</code></strong> —— 墙上时间 + IANA 时区（<code>Asia/Shanghai</code>）+ 偏移量。它既是绝对时刻也是墙上时间，两边都能读。</li> <li><strong><code>Duration</code></strong> —— 一段时间长度，不是"两个点之间的差"的近似，而是年/月/周/日/时/分/秒的独立量。</li></ul> <p><img src="https://bougieL.github.io/images/2026/09/26/fig2_001.jpg" alt="左右分屏构图：左边是暖色的挂钟显示本地时间，右边是冷色的 UTC 原子钟读数，冷暖光线对比强烈" loading="lazy"></p> <p>这个区分一旦做出来，前面那几个坑就自己消失了：</p> <ul><li>生日、节假日、"每周三下午三点开会" —— 这是 <code>PlainDate</code> / <code>PlainTime</code>，<strong>永远不该</strong>带时区。你不会希望一个出生日期因为用户换了时区就变成前一天。</li> <li>日志时间戳、订单创建时间、<code>expires_at</code> —— 这是 <code>Instant</code>，<strong>永远不该</strong>被当成墙上时间去做 "加一天"。</li> <li>用户可见的日程、航班起飞时间 —— 这是 <code>ZonedDateTime</code>，必须存 IANA 时区（不是偏移量），因为偏移量会变，时区规则也会变。</li></ul> <h2 id="三、几个高频场景的对照"> 三、几个高频场景的对照</h2> <h3 id="加一天"> 加一天</h3> <div class="language-js extra-class"><pre class="language-js"><code><span class="token comment">// 墙上时间加一天：永远是日历上的下一天</span>
Temporal<span class="token punctuation">.</span>PlainDate<span class="token punctuation">.</span><span class="token function">from</span><span class="token punctuation">(</span><span class="token string">'2026-03-07'</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">add</span><span class="token punctuation">(</span><span class="token punctuation">{</span> <span class="token literal-property property">days</span><span class="token operator">:</span> <span class="token number">1</span> <span class="token punctuation">}</span><span class="token punctuation">)</span>
<span class="token comment">// → 2026-03-08</span>

<span class="token comment">// 带时区加一天：墙上读数保持 12 点，但偏移量跟着变</span>
<span class="token keyword">const</span> zdt <span class="token operator">=</span> Temporal<span class="token punctuation">.</span>ZonedDateTime<span class="token punctuation">.</span><span class="token function">from</span><span class="token punctuation">(</span><span class="token string">'2026-03-07T12:00:00-05:00[America/New_York]'</span><span class="token punctuation">)</span>
zdt<span class="token punctuation">.</span><span class="token function">add</span><span class="token punctuation">(</span><span class="token punctuation">{</span> <span class="token literal-property property">days</span><span class="token operator">:</span> <span class="token number">1</span> <span class="token punctuation">}</span><span class="token punctuation">)</span>
<span class="token comment">// → 2026-03-08T12:00:00-04:00[America/New_York]   ← 仍是中午 12 点</span>

zdt<span class="token punctuation">.</span><span class="token function">add</span><span class="token punctuation">(</span><span class="token punctuation">{</span> <span class="token literal-property property">hours</span><span class="token operator">:</span> <span class="token number">24</span> <span class="token punctuation">}</span><span class="token punctuation">)</span>
<span class="token comment">// → 2026-03-08T13:00:00-04:00[America/New_York]   ← 绝对时间过了 24 小时，墙上变成 13 点</span>
</code></pre></div><p><code>days</code> 和 <code>hours</code> 在这里终于不再是同一个东西了。语义是被迫显式化的，这在我看来是 Temporal 最大的价值。</p> <h3 id="不存在的那个小时"> 不存在的那个小时</h3> <p>纽约 2026-03-08 的凌晨 2:30 是不存在的（2 点直接跳到 3 点）。Date 会安静地给你返回一个 3:30，Temporal 会直接抛错：</p> <div class="language-js extra-class"><pre class="language-js"><code>Temporal<span class="token punctuation">.</span>ZonedDateTime<span class="token punctuation">.</span><span class="token function">from</span><span class="token punctuation">(</span><span class="token string">'2026-03-08T02:30[America/New_York]'</span><span class="token punctuation">)</span>
<span class="token comment">// RangeError: ZonedDateTime does not exist</span>

<span class="token comment">// 你可以显式告诉它你想怎么办</span>
Temporal<span class="token punctuation">.</span>ZonedDateTime<span class="token punctuation">.</span><span class="token function">from</span><span class="token punctuation">(</span><span class="token string">'2026-03-08T02:30[America/New_York]'</span><span class="token punctuation">,</span> <span class="token punctuation">{</span> <span class="token literal-property property">disambiguation</span><span class="token operator">:</span> <span class="token string">'later'</span> <span class="token punctuation">}</span><span class="token punctuation">)</span>
<span class="token comment">// → 2026-03-08T03:30:00-04:00[America/New_York]</span>

Temporal<span class="token punctuation">.</span>ZonedDateTime<span class="token punctuation">.</span><span class="token function">from</span><span class="token punctuation">(</span><span class="token string">'2026-03-08T02:30[America/New_York]'</span><span class="token punctuation">,</span> <span class="token punctuation">{</span> <span class="token literal-property property">disambiguation</span><span class="token operator">:</span> <span class="token string">'reject'</span> <span class="token punctuation">}</span><span class="token punctuation">)</span>
<span class="token comment">// → 抛错，适合"配置错了就该报错"的场景</span>
</code></pre></div><p><code>disambiguation</code> 有四个值：<code>compatible</code>（默认，等价于 Date 的行为）、<code>earlier</code>、<code>later</code>、<code>reject</code>。同理，秋天"重复的那个小时"用 <code>offset</code> 选项来消歧。以前这些全靠运气。</p> <p><img src="https://bougieL.github.io/images/2026/09/26/fig3_001.jpg" alt="世界地图以发光的垂直时区条带呈现，其中一条被橙色高亮标出夏令时切换，深蓝背景" loading="lazy"></p> <h3 id="时区和偏移量不是一回事"> 时区和偏移量不是一回事</h3> <p>这是我以前一直混淆的点。<code>-05:00</code> 只是一个<strong>偏移量</strong>，美国东部冬天是 <code>-05:00</code>，夏天是 <code>-04:00</code>。<code>America/New_York</code> 是一个<strong>时区</strong>，它包含了"什么时候用哪个偏移量"的全部历史规则和未来规则。</p> <p>所以存数据库的时候，别存偏移量，存 IANA 时区名。Temporal 的 <code>ZonedDateTime</code> 把 IANA 时区放进方括号里就是在提醒你这件事：</p> <div class="language-js extra-class"><pre class="language-js"><code>Temporal<span class="token punctuation">.</span>ZonedDateTime<span class="token punctuation">.</span><span class="token function">from</span><span class="token punctuation">(</span><span class="token punctuation">{</span> <span class="token literal-property property">timeZone</span><span class="token operator">:</span> <span class="token string">'Asia/Shanghai'</span><span class="token punctuation">,</span> <span class="token literal-property property">year</span><span class="token operator">:</span> <span class="token number">2026</span><span class="token punctuation">,</span> <span class="token literal-property property">month</span><span class="token operator">:</span> <span class="token number">9</span><span class="token punctuation">,</span> <span class="token literal-property property">day</span><span class="token operator">:</span> <span class="token number">26</span><span class="token punctuation">,</span> <span class="token literal-property property">hour</span><span class="token operator">:</span> <span class="token number">20</span> <span class="token punctuation">}</span><span class="token punctuation">)</span>
<span class="token comment">// → 2026-09-26T20:00:00+08:00[Asia/Shanghai]</span>

Temporal<span class="token punctuation">.</span>Now<span class="token punctuation">.</span><span class="token function">zonedDateTimeISO</span><span class="token punctuation">(</span><span class="token string">'Asia/Shanghai'</span><span class="token punctuation">)</span>
<span class="token comment">// → 当前时刻，固定渲染在东八区</span>
</code></pre></div><h3 id="时间差和排序"> 时间差和排序</h3> <div class="language-js extra-class"><pre class="language-js"><code><span class="token keyword">const</span> a <span class="token operator">=</span> Temporal<span class="token punctuation">.</span>PlainDate<span class="token punctuation">.</span><span class="token function">from</span><span class="token punctuation">(</span><span class="token string">'2026-01-01'</span><span class="token punctuation">)</span>
<span class="token keyword">const</span> b <span class="token operator">=</span> Temporal<span class="token punctuation">.</span>PlainDate<span class="token punctuation">.</span><span class="token function">from</span><span class="token punctuation">(</span><span class="token string">'2026-09-26'</span><span class="token punctuation">)</span>

a<span class="token punctuation">.</span><span class="token function">until</span><span class="token punctuation">(</span>b<span class="token punctuation">)</span>                            <span class="token comment">// → P268D</span>
a<span class="token punctuation">.</span><span class="token function">until</span><span class="token punctuation">(</span>b<span class="token punctuation">,</span> <span class="token punctuation">{</span> <span class="token literal-property property">largestUnit</span><span class="token operator">:</span> <span class="token string">'months'</span> <span class="token punctuation">}</span><span class="token punctuation">)</span> <span class="token comment">// → P8M25D</span>
b<span class="token punctuation">.</span><span class="token function">since</span><span class="token punctuation">(</span>a<span class="token punctuation">)</span>                            <span class="token comment">// → P268D</span>

<span class="token comment">// 排序不用再写 getTime() 相减了</span>
list<span class="token punctuation">.</span><span class="token function">sort</span><span class="token punctuation">(</span>Temporal<span class="token punctuation">.</span>Instant<span class="token punctuation">.</span>compare<span class="token punctuation">)</span>
</code></pre></div><p><code>until</code> / <code>since</code> 返回的是 <code>Duration</code>，可以再 <code>round</code>、<code>total('hours')</code>、<code>with</code>。注意"一个月是多少天"这个问题没有唯一答案，所以 Duration 里的月和日不会自动互转——除非你给一个 <code>relativeTo</code>。</p> <h3 id="和-date-互转"> 和 Date 互转</h3> <p>迁移期免不了打交道，转换很直接：</p> <div class="language-js extra-class"><pre class="language-js"><code><span class="token keyword">const</span> instant <span class="token operator">=</span> Temporal<span class="token punctuation">.</span>Instant<span class="token punctuation">.</span><span class="token function">fromEpochMilliseconds</span><span class="token punctuation">(</span>date<span class="token punctuation">.</span><span class="token function">getTime</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span>
<span class="token keyword">const</span> back <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">Date</span><span class="token punctuation">(</span>instant<span class="token punctuation">.</span>epochMilliseconds<span class="token punctuation">)</span>

<span class="token comment">// 或者走字符串</span>
Temporal<span class="token punctuation">.</span>Instant<span class="token punctuation">.</span><span class="token function">from</span><span class="token punctuation">(</span>date<span class="token punctuation">.</span><span class="token function">toISOString</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span>
</code></pre></div><h2 id="四、现在能直接用吗"> 四、现在能直接用吗</h2> <p>这里要说实话：<strong>看你的运行环境。</strong></p> <p>按目前公开的信息（写这篇的时候），Temporal 在 2026 年 3 月的 TC39 会议上进入 Stage 4，正式成为 ES2026 的一部分；Chrome / Edge 从 144 版本（2026 年 1 月）起默认开启；Firefox 也已经跟上；Safari 的进度相对靠后。Node.js 26（2026 年 5 月发布，同年 10 月进 LTS）默认启用了 Temporal。</p> <p>也就是说：<strong>服务端（新版 Node）现在基本可以直接写，浏览器端要看你的用户分布。</strong></p> <p><img src="https://bougieL.github.io/images/2026/09/26/fig4_001.jpg" alt="整洁的开发者工作台，屏幕上显示着现代 JavaScript 日期与时长代码，旁边漂浮着日历控件和时间轴条，柔和自然光" loading="lazy"></p> <p>要用的话，路径大概是这样的：</p> <ol><li><strong>新代码优先用 Temporal，旧代码别急着动。</strong> 这两套东西可以并存，转换成本很低（就是上面那两行）。</li> <li><strong>需要兼容老浏览器就上 polyfill</strong>（<code>@js-temporal/polyfill</code>）。它把 <code>Temporal</code> 挂到全局，业务代码不用改。代价是体积——它要打包完整的时区与日历数据，gzip 后仍是几十 KB 量级，比 dayjs 大不少。如果只是为了"计算"，可以考虑只在新代码里用、用构建时的能力检测做条件加载。</li> <li><strong>moment.js 可以先退了。</strong> moment 当年解决的正是 Date 的这些坑（不可变、链式、时区、格式化），现在这些能力原生都有了，它作为"补丁"的使命基本结束。dayjs / date-fns 也依然有用——它们更轻，而且在 Temporal 覆盖不到的地方（比如复杂的自定义格式化）仍然省事。</li> <li><strong>最重要的一条：先把"这个字段是墙上时间还是绝对时刻"这件事在代码里显式化。</strong> 就算暂时不上 Temporal，把 <code>PlainDate</code> 和 <code>Instant</code> 的区分用命名、注释或者类型标注先表达出来，也已经能挡掉大半的 bug。</li></ol> <h2 id="五、一点感想"> 五、一点感想</h2> <p>我一开始对 Temporal 是有抵触的：Date 我用了十年，闭着眼都能写，现在告诉我这套东西不对，还要多记七八个类型名。</p> <p>但真把它用在那个倒计时 bug 上之后，我的感受变了。以前我要在脑子里一直维护一条隐含规则："这里加的是 24 小时还是一天来着？这个时间是本地还是 UTC？" 现在这条规则被搬到了类型系统里——<code>add({ days: 1 })</code> 和 <code>add({ hours: 24 })</code> 写在代码里就是两行不一样的东西，读代码的人一眼能看出区别。</p> <p>好的 API 不是让你少打字，是<strong>让你没办法不小心写错</strong>。Date 的所有问题本质上都是同一件事：它把两种不同的东西当成了一种。Temporal 花了八年（2018 年进 Stage 1，到 2026 年进 Stage 4）才把这笔三十年的老账理清楚，慢是慢了点，但这次是真的还清了。</p> <hr> <p>（文中浏览器与 Node 的支持情况截至本文写作时，实际使用前请以当前版本的实测结果为准。）</p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[密码正在退场：Passkey、WebAuthn，和那些没人告诉我的坑]]></title>
            <description><![CDATA[我做登录页做了快十年，一直以为「密码 + 短信验证码」是这个问题的终点。直到真的把 WebAuthn 接上线，才发现密码不是被「加强」了，而是被换掉了——服务器手里从秘密变成了公钥。这篇记一下这个替换是怎么发生的、代码长什么样，以及同步这件事带来的新麻烦。]]></description>
            <link>https://bougieL.github.io/2026/09/23/passkey-end-of-password/</link>
            <guid isPermaLink="true">https://bougieL.github.io/2026/09/23/passkey-end-of-password/</guid>
            <pubDate>Wed, 23 Sep 2026 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<p>上个月我收到一封邮件，开头是那句熟悉的话：「我们检测到您的账户密码可能已泄露，请立即修改。」</p> <p>我很配合地点进去改了一个，然后顺手打开那个存了几百条记录的密码管理器，看着里面一半以上的条目都带着「建议更换」的黄色小三角。那一刻我意识到一个有点荒谬的事实：我是一个写了十年登录页的人，而我自己的数字生活，仍然建立在「把同一串字符交给几十个网站各自存一份」这个模型上。</p> <p><img src="https://bougieL.github.io/images/2026/09/23/cover_001.jpg" alt="一张散落着写满密码又被划掉的纸条的暗色桌面，上方浮着一部显示生物识别登录提示的手机" loading="lazy"></p> <p>真正让我动手的，是 FIDO Alliance 在今年 5 月 World Passkey Day 发布的《State of Passkeys 2026》：他们估计全球已经有 <strong>50 亿个 passkey</strong> 在使用；90% 的人听说过 passkey；75% 的人至少在一个账户上启用了它；49% 的人在支持的地方会经常用。企业侧也有 68% 已经部署或正在部署。</p> <p>这些数字里最扎眼的其实不是 adoption，而是另一条：过去一年里，<strong>三分之一的人（33%）经历过账户被盗或收到过泄露通知</strong>。密码这套东西，到今天还在实实在在地伤人。</p> <h2 id="密码的问题-从来不是「不够复杂」"> 密码的问题，从来不是「不够复杂」</h2> <p>我们对付密码的手段一直是补丁式的：要求大写小写数字符号、定期更换、再加一条短信验证码。但这些补丁都没碰到底层那个结构性问题——</p> <p><strong>密码是一个共享秘密。</strong></p> <p>你要登录，就得把秘密发给服务器；服务器必须保存一份可以用来验证它的东西（哪怕是哈希）。这意味着：</p> <ul><li>服务器被拖库 → 秘密的残留物就流出去了，可以离线爆破、可以撞库；</li> <li>你在一个长得一模一样的页面上输入了它 → 秘密直接交给了攻击者，验证码也一样能转发；</li> <li>中间人、键盘记录器、浏览器插件 → 拿到的是秘密本身，用不用得上只取决于它有没有过期。</li></ul> <p><img src="https://bougieL.github.io/images/2026/09/23/fig1_001.jpg" alt="暗色机房里一排屏幕，铺满泄露的邮箱与密码记录，红色告警灯" loading="lazy"></p> <p>Passkey 换掉的是这一整条链路的核心：<strong>服务器不再持有秘密，只持有公钥。</strong></p> <p>这一个改动同时干掉了上面三类攻击。数据库里全是公钥，拖走也没用；钓鱼站拿不到能用的东西，因为签名是浏览器按「当前访问的域名」去要的，假域名要不到真站点的签名；中间人截获的那串字节是一次性签名，重放无效。</p> <h2 id="passkey-到底是个什么东西"> Passkey 到底是个什么东西</h2> <p>先拆掉一个常见误解：passkey 不是「指纹登录」的另一种说法。生物识别只是<strong>解锁本地私钥</strong>的方式，跟协议无关——你用 PIN、用图案、用指纹，对服务器来说都一样，它只看到一个签名。</p> <p>技术上，一个 passkey 是一对非对称密钥：</p> <p><img src="https://bougieL.github.io/images/2026/09/23/fig2_001.jpg" alt="暗色桌面微距：一枚发光的黄铜公钥与一枚被封在安全芯片里的哑光私钥" loading="lazy"></p> <ul><li><strong>私钥</strong>：生成后存在设备的 Secure Enclave / TPM / Android Keystone 里，或者存在密码管理器（1Password、Bitwarden、iCloud 钥匙串）里。它永远不出那个安全边界；</li> <li><strong>公钥</strong>：注册时交给服务器，跟 user id 一起存下来，就是数据库里那一行。</li></ul> <p>WebAuthn 里有个专门的叫法：<strong>discoverable credential</strong>（以前叫 resident key）。意思是凭据本身自带用户信息，登录时不需要先输入用户名——服务器发一个空的用户名挑战，浏览器自己列出「这个域名下你有哪些 passkey」让你选。这正是 passkey 和普通 WebAuthn 第二因素的区别：后者你得先输密码，再摸一下钥匙；前者<strong>它自己就是完整的认证</strong>。</p> <p>底层是 FIDO2 = WebAuthn（浏览器 API，W3C）+ CTAP（浏览器和 authenticator 之间的协议）。你在浏览器里只碰得到前者。</p> <h2 id="一次注册、一次登录-实际发生了什么"> 一次注册、一次登录，实际发生了什么</h2> <p>我把接入的代码简化了一遍。先看注册，服务端先给一组选项：</p> <div class="language-js extra-class"><pre class="language-js"><code><span class="token comment">// Node / SimpleWebAuthn</span>
<span class="token keyword">const</span> options <span class="token operator">=</span> <span class="token keyword">await</span> <span class="token function">generateRegistrationOptions</span><span class="token punctuation">(</span><span class="token punctuation">{</span>
  <span class="token literal-property property">rpName</span><span class="token operator">:</span> <span class="token string">"Bougie's Blog"</span><span class="token punctuation">,</span>
  <span class="token literal-property property">rpID</span><span class="token operator">:</span> <span class="token string">"bougieL.github.io"</span><span class="token punctuation">,</span>
  <span class="token literal-property property">userID</span><span class="token operator">:</span> Buffer<span class="token punctuation">.</span><span class="token function">from</span><span class="token punctuation">(</span>user<span class="token punctuation">.</span>id<span class="token punctuation">)</span><span class="token punctuation">,</span> <span class="token comment">// 稳定、不可变</span>
  <span class="token literal-property property">userName</span><span class="token operator">:</span> user<span class="token punctuation">.</span>email<span class="token punctuation">,</span>
  <span class="token literal-property property">userDisplayName</span><span class="token operator">:</span> user<span class="token punctuation">.</span>name<span class="token punctuation">,</span>
  <span class="token literal-property property">attestationType</span><span class="token operator">:</span> <span class="token string">"none"</span><span class="token punctuation">,</span>
  <span class="token literal-property property">excludeCredentials</span><span class="token operator">:</span> existing<span class="token punctuation">.</span><span class="token function">map</span><span class="token punctuation">(</span><span class="token punctuation">(</span><span class="token parameter">c</span><span class="token punctuation">)</span> <span class="token operator">=&gt;</span> <span class="token punctuation">(</span><span class="token punctuation">{</span> <span class="token literal-property property">id</span><span class="token operator">:</span> c<span class="token punctuation">.</span>credentialID<span class="token punctuation">,</span> <span class="token literal-property property">transports</span><span class="token operator">:</span> c<span class="token punctuation">.</span>transports <span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">,</span>
  <span class="token literal-property property">authenticatorSelection</span><span class="token operator">:</span> <span class="token punctuation">{</span>
    <span class="token literal-property property">residentKey</span><span class="token operator">:</span> <span class="token string">"required"</span><span class="token punctuation">,</span> <span class="token comment">// 要的就是 discoverable</span>
    <span class="token literal-property property">userVerification</span><span class="token operator">:</span> <span class="token string">"preferred"</span><span class="token punctuation">,</span>
  <span class="token punctuation">}</span><span class="token punctuation">,</span>
<span class="token punctuation">}</span><span class="token punctuation">)</span>
<span class="token comment">// 把 challenge 存进 session，一次性</span>
session<span class="token punctuation">.</span>challenge <span class="token operator">=</span> options<span class="token punctuation">.</span>challenge
</code></pre></div><p>浏览器侧只有一句话：</p> <div class="language-js extra-class"><pre class="language-js"><code><span class="token keyword">const</span> credential <span class="token operator">=</span> <span class="token keyword">await</span> navigator<span class="token punctuation">.</span>credentials<span class="token punctuation">.</span><span class="token function">create</span><span class="token punctuation">(</span><span class="token punctuation">{</span> <span class="token literal-property property">publicKey</span><span class="token operator">:</span> options <span class="token punctuation">}</span><span class="token punctuation">)</span>
<span class="token keyword">await</span> <span class="token function">fetch</span><span class="token punctuation">(</span><span class="token string">"/api/passkey/register"</span><span class="token punctuation">,</span> <span class="token punctuation">{</span>
  <span class="token literal-property property">method</span><span class="token operator">:</span> <span class="token string">"POST"</span><span class="token punctuation">,</span>
  <span class="token literal-property property">body</span><span class="token operator">:</span> <span class="token constant">JSON</span><span class="token punctuation">.</span><span class="token function">stringify</span><span class="token punctuation">(</span>credential<span class="token punctuation">)</span><span class="token punctuation">,</span>
<span class="token punctuation">}</span><span class="token punctuation">)</span>
</code></pre></div><p>注意 <code>options</code> 里的 challenge 必须是 <code>Uint8Array</code>，而服务端生成的通常是 base64url 字符串，客户端要做一次转换（SimpleWebAuthn 的 <code>startRegistration</code> 帮你做了，手写的话别漏）。</p> <p>服务端拿到 <code>credential</code> 之后做验证：</p> <div class="language-js extra-class"><pre class="language-js"><code><span class="token keyword">const</span> verification <span class="token operator">=</span> <span class="token keyword">await</span> <span class="token function">verifyRegistrationResponse</span><span class="token punctuation">(</span><span class="token punctuation">{</span>
  <span class="token literal-property property">response</span><span class="token operator">:</span> credential<span class="token punctuation">,</span>
  <span class="token literal-property property">expectedChallenge</span><span class="token operator">:</span> session<span class="token punctuation">.</span>challenge<span class="token punctuation">,</span>
  <span class="token literal-property property">expectedOrigin</span><span class="token operator">:</span> <span class="token string">"https://bougieL.github.io"</span><span class="token punctuation">,</span>
  <span class="token literal-property property">expectedRPID</span><span class="token operator">:</span> <span class="token string">"bougieL.github.io"</span><span class="token punctuation">,</span>
<span class="token punctuation">}</span><span class="token punctuation">)</span>
<span class="token comment">// 然后把 credentialID + publicKey + counter 存进数据库</span>
</code></pre></div><p>登录是镜像的流程，<code>navigator.credentials.get()</code> 拿一个 assertion，服务端验签。这里我加了一个我觉得体验提升最大的东西——<strong>Conditional UI</strong>，让 passkey 直接出现在用户名输入框的自动填充里，不需要用户先点「使用 passkey 登录」：</p> <div class="language-html extra-class"><pre class="language-html"><code><span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>input</span> <span class="token attr-name">type</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">"</span>text<span class="token punctuation">"</span></span> <span class="token attr-name">name</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">"</span>username<span class="token punctuation">"</span></span> <span class="token attr-name">autocomplete</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">"</span>username webauthn<span class="token punctuation">"</span></span> <span class="token punctuation">/&gt;</span></span>
</code></pre></div><div class="language-js extra-class"><pre class="language-js"><code><span class="token keyword">const</span> credential <span class="token operator">=</span> <span class="token keyword">await</span> navigator<span class="token punctuation">.</span>credentials<span class="token punctuation">.</span><span class="token function">get</span><span class="token punctuation">(</span><span class="token punctuation">{</span>
  <span class="token literal-property property">publicKey</span><span class="token operator">:</span> <span class="token punctuation">{</span> challenge<span class="token punctuation">,</span> rpId<span class="token punctuation">,</span> <span class="token literal-property property">userVerification</span><span class="token operator">:</span> <span class="token string">"preferred"</span> <span class="token punctuation">}</span><span class="token punctuation">,</span>
  <span class="token literal-property property">mediation</span><span class="token operator">:</span> <span class="token string">"conditional"</span><span class="token punctuation">,</span> <span class="token comment">// 关键</span>
<span class="token punctuation">}</span><span class="token punctuation">)</span>
</code></pre></div><p><img src="https://bougieL.github.io/images/2026/09/23/fig3_001.jpg" alt="抽象示意图：左侧笔记本与右侧安全密钥之间，流动着发光的挑战-响应光带" loading="lazy"></p> <h2 id="服务端到底验了哪几件事"> 服务端到底验了哪几件事</h2> <p>这部分是我接的时候最想有人给我讲清楚的。一次登录，服务端手里的输入只有三段字节：<code>clientDataJSON</code>、<code>authenticatorData</code>、<code>signature</code>。校验大致七步：</p> <ol><li><code>clientDataJSON.type</code> 必须是 <code>webauthn.get</code>（注册时是 <code>webauthn.create</code>）——防止拿注册响应冒充登录；</li> <li><code>clientDataJSON.challenge</code> 等于 session 里那个，且<strong>用过即删</strong>；</li> <li><code>clientDataJSON.origin</code> 等于预期的完整 origin，不能只比域名；</li> <li><code>authenticatorData</code> 前 32 字节是 <code>rpIdHash</code>，必须等于 <code>SHA256(rpID)</code>——<strong>这一条就是抗钓鱼的全部依据</strong>；</li> <li>flags 字节里 UP（用户在场）必须置位，如果请求时写了 <code>userVerification: "required"</code>，UV 位也要置位；</li> <li>用数据库里存的 COSE 公钥验签：<code>authenticatorData || SHA256(clientDataJSON)</code>；</li> <li><code>signCount</code> 必须比上次大。</li></ol> <p>第 7 条有个坑：<strong>同步型 passkey 的 signCount 常常永远是 0</strong>。它设计出来是为了检测克隆的硬件钥匙，但同步凭据本来就在多台设备上合法存在，计数器没有意义。所以别把它写成硬性失败，记录 + 告警就够了。</p> <p>flags 里还有两位值得单独拎出来：<strong>BE（backup eligible，可备份）</strong> 和 <strong>BS（backup state，已被备份）</strong>。它们告诉你这个凭据是不是同步型的——后面会讲到，这两位现在比 signCount 有用得多。</p> <h2 id="同步-是它最大的优点-也是最大的争议"> 同步，是它最大的优点，也是最大的争议</h2> <p>最初的 FIDO 模型里，私钥是<strong>硬件绑定</strong>的：它在那枚钥匙里，拿不走。这也意味着丢掉钥匙 = 丢掉账户。</p> <p>后来苹果和谷歌做了一件改变格局的事：让 passkey 通过 iCloud 钥匙串 / Google 密码管理器在设备之间同步。于是你现在在 iPhone 上注册的 passkey，可以在 Mac 上直接用；换手机也不至于当场锁死自己。</p> <p><img src="https://bougieL.github.io/images/2026/09/23/fig4_001.jpg" alt="深色空间中，笔记本、手机、平板由柔和的同步光线连成一个三角形" loading="lazy"></p> <p>代价是威胁模型变了：</p> <ul><li>私钥现在存在云端，且（各家实现不同）由 Apple / Google 的账户安全保护。<strong>你的 Apple ID / Google 账号成了新的单点</strong>——它被攻破，攻击者能拿到的是你所有的 passkey，而不只是一个网站的密码；</li> <li>「不可导出」这个硬件保证没有了，企业合规场景里那些「凭据必须绑定受管设备」的要求就没法靠默认 passkey 满足。</li></ul> <p>所以 BE/BS 那两位变得重要：企业可以在注册时要求 device-bound（BE=0），或者用 attestation + AAGUID 白名单来限定允许的 authenticator 型号。个人站点则完全相反——同步是好事，别去拦。</p> <p>我的判断是：<strong>对绝大多数消费级场景，同步 passkey 仍然远比密码安全</strong>。攻击者要偷你的 iCloud 钥匙串，得先突破一个开启了 2FA 的 Apple ID；而偷密码只需要你点错一次链接。真正的风险排序里，钓鱼的量级比账号接管高太多了。</p> <h2 id="_2026-年-锁定的问题终于在被解决"> 2026 年，锁定的问题终于在被解决</h2> <p>我去年不敢把 passkey 推荐给家人的一个原因是：<strong>它太容易把人锁在门外</strong>。同一生态内很丝滑，跨平台就很尴尬，而各家又不让你把凭据导出。</p> <p>今年这块松动了：</p> <ul><li><strong>2026 年 8 月 25 日，WebAuthn Level 3 正式成为 W3C Recommendation</strong>。这意味着多设备 passkey 这一整套行为终于从「浏览器各自实现」变成了稳定标准（新特性会进 Level 4）。对开发者的意义很直接：可以放心依赖，不用再为每个版本差异写兼容分支；</li> <li>FIDO Alliance 发布了 <strong>Credential Exchange Protocol（CXP）与 Format（CXF）</strong>，定义了凭据在管理器之间搬运的标准格式。今年 7 月，包括几家主流密码管理器在内的厂商联合宣布支持；</li> <li>据公开报道，苹果也在 iOS 26 / macOS 26 这一代系统里加入了 passkey 的<strong>导入导出</strong>。</li></ul> <p>跨生态迁移这条最难的路，第一次有了标准答案。</p> <h2 id="一些只有接了线才会知道的事"> 一些只有接了线才会知道的事</h2> <p><strong>challenge 必须在服务端生成且一次性。</strong> 我第一版图省事把它存在了 localStorage 里，等于自己把抗重放的唯一防线拆了。</p> <p><strong>rpID 决定了你的 passkey 作用域。</strong> 它必须是当前域名的可注册后缀。<code>app.example.com</code> 可以把 rpID 设为 <code>example.com</code>，这样 <code>www.example.com</code> 也能用同一批凭据。但如果你想让两个完全不同的域名共用一套 passkey（比如换了主域），就得用 <strong>Related Origin Requests</strong>：在 <code>example.com/.well-known/webauthn</code> 上放一份 JSON，列出允许的 origin。</p> <div class="language-json extra-class"><pre class="language-json"><code><span class="token punctuation">{</span>
  <span class="token property">"origins"</span><span class="token operator">:</span> <span class="token punctuation">[</span><span class="token string">"https://app.example.com"</span><span class="token punctuation">,</span> <span class="token string">"https://www.example.com"</span><span class="token punctuation">]</span>
<span class="token punctuation">}</span>
</code></pre></div><p><strong>attestation 基本不用开。</strong> 除了企业场景需要确认硬件型号，普通站点设 <code>attestation: "none"</code> 就好——开了反而会让部分用户看到额外的隐私授权弹窗，而且为了隐私，很多 authenticator 返回的 AAGUID 是全零。</p> <p><strong>跨设备登录（hybrid）是自动的。</strong> 手机不在手边时，用户可以用电脑上的二维码扫手机完成认证，走的是蓝牙 + 距离校验。你不需要写代码，只要别把 <code>transports</code> 写死成 <code>internal</code>。</p> <p><strong>登录流程改造要渐进。</strong> 我的顺序是：先让已有用户在「安全设置」里自助添加 passkey（这时候信任成本最低），跑一段时间，再把登录页的默认路径换成 conditional UI，密码保留为兜底。一次性切换的结果一定是客服工单爆炸。</p> <h2 id="密码不会在某一天消失"> 密码不会在某一天消失</h2> <p>写完之后我的判断是这样的：</p> <p>passkey 是一个<strong>罕见的、安全和体验不冲突的</strong>升级——更快（点一下 vs 输 12 个字符）、更安全（抗钓鱼、服务器无秘密可泄露）、还省掉了短信验证码的钱和延迟。这种好事在安全领域不多见。</p> <p>但 FIDO 那份报告里还有一条冷静的数据：<strong>57% 的组织，员工日常登录仍然依赖可被钓鱼的方式</strong>。密码的退场不会是一个开关，而会是一个很长的渐变——共享设备、老年用户、客服代登、遗留系统、极端合规场景，都会让它继续存在好些年。</p> <p>对个人来说，这件事其实很简单：<strong>在支持的地方，把 passkey 开上</strong>。它不会让你更安全到无懈可击，但它把你从「记住几百个秘密」这件人类本来就不擅长的事里，解放出来了。</p> <p>至于我那个密码管理器——我还没删。里面有四十多个站点不支持 passkey，其中一个是我交电费的地方。</p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[把压缩代码翻译回人话：Source Map 里到底装了什么]]></title>
            <description><![CDATA[线上报错只留给我一个数字：app.a8f3c2.js:1:38247。我盯着它看了三分钟才反应过来——这个数字本身毫无意义，让它变成 src/utils/format.js:42:11 的，是一个用了十几年、我却从没认真读过的格式。这篇把它拆开。]]></description>
            <link>https://bougieL.github.io/2026/09/22/source-map-anatomy/</link>
            <guid isPermaLink="true">https://bougieL.github.io/2026/09/22/source-map-anatomy/</guid>
            <pubDate>Tue, 22 Sep 2026 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<p>上周三下午，报警群里弹出一条：</p> <div class="language- extra-class"><pre class="language-text"><code>TypeError: Cannot read properties of undefined (reading 'name')
    at o (app.a8f3c2.js:1:38247)
</code></pre></div><p>就这些。没有文件名，没有函数名（那个 <code>o</code> 是压缩后的），只有一个列号：<code>38247</code>。</p> <p>我在群里回了句「我看下」，然后打开了构建产物。第一行有 12 万个字符，我把它拷进编辑器，把光标挪到第 38247 列——屏幕中间是一段毫无特征的代码，前后都是 <code>function(e,t,n){return...}</code> 这种东西。看不出来是谁。</p> <p><img src="https://bougieL.github.io/images/2026/09/22/cover_001.jpg" alt="一张旧牛皮纸地图摊在暗色桌面上，上面浮着淡淡的发光代码行" loading="lazy"></p> <p>真正救我的是另一个文件：<code>app.a8f3c2.js.map</code>。</p> <h2 id="它其实只是一份-json"> 它其实只是一份 JSON</h2> <p>报警后台那一屏红色的东西，有用的只有最后那个列号。而这个列号要被翻译成人话，全靠一个纯文本文件。</p> <p><img src="https://bougieL.github.io/images/2026/09/22/fig1_001.jpg" alt="暗色房间里一块显示器，屏幕上是一段红色的报错堆栈" loading="lazy"></p> <p>我拿一个几行的例子把它跑了一遍。原始文件 <code>demo.js</code>：</p> <div class="language-js extra-class"><pre class="language-js"><code><span class="token keyword">function</span> <span class="token function">greet</span><span class="token punctuation">(</span><span class="token parameter">name</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
  <span class="token keyword">const</span> message <span class="token operator">=</span> <span class="token string">"hello, "</span> <span class="token operator">+</span> name
  <span class="token keyword">return</span> message<span class="token punctuation">.</span><span class="token function">toUpperCase</span><span class="token punctuation">(</span><span class="token punctuation">)</span>
<span class="token punctuation">}</span>

console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span><span class="token function">greet</span><span class="token punctuation">(</span><span class="token string">"bougie"</span><span class="token punctuation">)</span><span class="token punctuation">)</span>
</code></pre></div><p>用 terser 压一遍，产物是一行：</p> <div class="language-js extra-class"><pre class="language-js"><code><span class="token keyword">function</span> <span class="token function">greet</span><span class="token punctuation">(</span><span class="token parameter">e</span><span class="token punctuation">)</span><span class="token punctuation">{</span><span class="token keyword">return</span><span class="token punctuation">(</span><span class="token string">"hello, "</span><span class="token operator">+</span>e<span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">toUpperCase</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">}</span>console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span><span class="token function">greet</span><span class="token punctuation">(</span><span class="token string">"bougie"</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre></div><p>同时生成的 <code>demo.min.js.map</code>，去掉换行长这样：</p> <div class="language-json extra-class"><pre class="language-json"><code><span class="token punctuation">{</span>
  <span class="token property">"version"</span><span class="token operator">:</span> <span class="token number">3</span><span class="token punctuation">,</span>
  <span class="token property">"sources"</span><span class="token operator">:</span> <span class="token punctuation">[</span><span class="token string">"demo.js"</span><span class="token punctuation">]</span><span class="token punctuation">,</span>
  <span class="token property">"names"</span><span class="token operator">:</span> <span class="token punctuation">[</span><span class="token string">"greet"</span><span class="token punctuation">,</span> <span class="token string">"name"</span><span class="token punctuation">,</span> <span class="token string">"toUpperCase"</span><span class="token punctuation">,</span> <span class="token string">"console"</span><span class="token punctuation">,</span> <span class="token string">"log"</span><span class="token punctuation">]</span><span class="token punctuation">,</span>
  <span class="token property">"mappings"</span><span class="token operator">:</span> <span class="token string">"AAAA,SAASA,MAAMC,GAEb,OADgB,UAAYA,GACbC,cAGjBC,QAAQC,IAAIJ,MAAM"</span>
<span class="token punctuation">}</span>
</code></pre></div><p>字段不多，值得逐个说一句：</p> <ul><li><code>version</code>：目前就是 3。这份格式是 2011 年 Google 定的（为了 Closure Compiler），十几年了没怎么动过；</li> <li><code>sources</code>：原始文件列表，映射里的「源文件索引」指向的就是它；</li> <li><code>names</code>：压缩前出现过的标识符。上面那个例子里，<code>name</code>、<code>toUpperCase</code>、<code>console</code>、<code>log</code> 全在里面；</li> <li><code>sourcesContent</code>：可选，把源码原文整份塞进来，这样 DevTools 不用再回头去请求源文件，离线也能看；</li> <li><code>mappings</code>：真正干活的字段，也是整份文件里唯一看不懂的那个。</li></ul> <p>顺手记了三个数字：源码 121 字节，压缩产物 115 字节，map 167 字节。<strong>map 比代码本身还大。</strong> 这跟大多数人的直觉是反的——我们总觉得它是个附属品，其实它经常是构建产物里最占地方的那部分。</p> <h2 id="mappings-一串看起来像乱码的东西"> mappings：一串看起来像乱码的东西</h2> <div class="language- extra-class"><pre class="language-text"><code>AAAA,SAASA,MAAMC,GAEb,OADgB,UAAYA,GACbC,cAGjBC,QAAQC,IAAIJ,MAAM
</code></pre></div><p>两个分隔符，规则很简单：</p> <ul><li><code>;</code> 分隔<strong>生成文件的行</strong>（上面只有一行，所以一个分号都没有）；</li> <li><code>,</code> 分隔一行里的<strong>段</strong>（segment），每一段记录一次「生成位置 ↔ 原始位置」的对应。</li></ul> <p>每一段由 1 个、4 个或 5 个数字组成，顺序是：</p> <ol><li>生成文件的列（相对上一个段）</li> <li>源文件索引（相对上一个段）</li> <li>原始文件行（相对上一个段）</li> <li>原始文件列（相对上一个段）</li> <li><code>names</code> 索引（相对上一个段，可选）</li></ol> <p><img src="https://bougieL.github.io/images/2026/09/22/fig2_001.jpg" alt="一卷打孔纸带盘在暗色台面上，带子上是密密麻麻的小孔和符号" loading="lazy"></p> <p>拿第二段 <code>SAASA</code> 拆一遍：</p> <table><thead><tr><th>字符</th> <th>base64 值</th> <th>解出来</th> <th>累加后</th> <th>含义</th></tr></thead> <tbody><tr><td><code>S</code></td> <td>18</td> <td>+9</td> <td>9</td> <td>生成列 9</td></tr> <tr><td><code>A</code></td> <td>0</td> <td>+0</td> <td>0</td> <td>源文件 0</td></tr> <tr><td><code>A</code></td> <td>0</td> <td>+0</td> <td>0</td> <td>原始行 0</td></tr> <tr><td><code>S</code></td> <td>18</td> <td>+9</td> <td>9</td> <td>原始列 9</td></tr> <tr><td><code>A</code></td> <td>0</td> <td>+0</td> <td>0</td> <td>names[0]</td></tr></tbody></table> <p>也就是：<strong>压缩产物第 1 行第 9 列，来自 <code>demo.js</code> 第 1 行第 9 列，那个位置的标识符叫 <code>greet</code>。</strong></p> <p>生成文件第 9 列是 <code>function greet(e)</code> 里 <code>greet</code> 的 <code>g</code>，源文件第 9 列也是 <code>greet</code> 的 <code>g</code>。对得上。</p> <p>再看第三段 <code>MAAMC</code>：</p> <table><thead><tr><th>字符</th> <th>解出来</th> <th>累加后</th> <th>含义</th></tr></thead> <tbody><tr><td><code>M</code></td> <td>+6</td> <td>15</td> <td>生成列 15</td></tr> <tr><td><code>A</code></td> <td>+0</td> <td>0</td> <td>源文件 0</td></tr> <tr><td><code>A</code></td> <td>+0</td> <td>0</td> <td>原始行 0</td></tr> <tr><td><code>M</code></td> <td>+6</td> <td>15</td> <td>原始列 15</td></tr> <tr><td><code>C</code></td> <td>+1</td> <td>1</td> <td>names[1]</td></tr></tbody></table> <p>生成文件第 15 列是 <code>function greet(e)</code> 里的 <code>e</code>——压缩后它叫 <code>e</code>。而源文件第 15 列是 <code>function greet(name)</code> 里的 <code>name</code>，<code>names[1]</code> 也正好是 <code>name</code>。</p> <p><strong>第 5 个字段就是干这个的</strong>：让被改名的变量在报错堆栈里能变回它原来的名字。少这一个字段，你只能知道「在原始文件的第 15 列」，不知道那个东西原本叫什么。</p> <h2 id="为什么全是相对偏移"> 为什么全是相对偏移</h2> <p>注意上面每个数字都是「相对上一个段」的增量，不是绝对值。</p> <p><img src="https://bougieL.github.io/images/2026/09/22/fig3_001.jpg" alt="一卷卷尺摊在工作台上，旁边散着几块写着数字的小木块" loading="lazy"></p> <p>因为绝对列号会很大——回到开头那条报警，<code>38247</code>。而相邻两个映射点之间的距离通常只有个位数：一段语句里的下一个 token，往前挪 6 列、9 列而已。</p> <p>数字越小，编码越短。这是整个格式设计里最关键的一个决定：它把一列映射的成本压到了几个字符，所以一份 map 才能塞下几十万个映射点还不至于失控。</p> <p>代价是<strong>解码必须从头开始</strong>。你不能随机访问第 5000 个段，得把前面 4999 个都走过一遍。所以 <code>source-map</code> 那些库在消费 map 时都是一次性全量解码，再建索引。</p> <h2 id="vlq-以及那套-base64-字符"> VLQ，以及那套 base64 字符</h2> <p>每个数字用的是 <strong>VLQ（Variable Length Quantity）+ base64</strong>：一个字符 6 个 bit，低 5 位存数据，最高位（值 32）是「后面还有」的续行标志。</p> <div class="language- extra-class"><pre class="language-text"><code>字符:  A      B      C  ...  a      b  ...  0      9      +      /
索引:  0      1      2      26     27      52     61     62     63
</code></pre></div><p>字符顺序就是标准 base64（<code>A-Za-z0-9+/</code>），但<strong>符号不在这里面</strong>：VLQ 把符号塞进了最后一个 bit 的最低位。所以解出来的数要右移一位，最低位是 1 就取负。</p> <p>手写一个解码器不长，二十来行：</p> <div class="language-js extra-class"><pre class="language-js"><code><span class="token keyword">const</span> <span class="token constant">B64</span> <span class="token operator">=</span> <span class="token string">'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/'</span>

<span class="token comment">// 解码一个 base64 VLQ 数字，返回 [值, 下一个字符的下标]</span>
<span class="token keyword">function</span> <span class="token function">decodeVLQ</span><span class="token punctuation">(</span><span class="token parameter">str<span class="token punctuation">,</span> i</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
  <span class="token keyword">let</span> result <span class="token operator">=</span> <span class="token number">0</span>
  <span class="token keyword">let</span> shift <span class="token operator">=</span> <span class="token number">0</span>
  <span class="token keyword">let</span> digit
  <span class="token keyword">do</span> <span class="token punctuation">{</span>
    digit <span class="token operator">=</span> <span class="token constant">B64</span><span class="token punctuation">.</span><span class="token function">indexOf</span><span class="token punctuation">(</span>str<span class="token punctuation">[</span>i<span class="token operator">++</span><span class="token punctuation">]</span><span class="token punctuation">)</span>
    result <span class="token operator">+=</span> <span class="token punctuation">(</span>digit <span class="token operator">&amp;</span> <span class="token number">31</span><span class="token punctuation">)</span> <span class="token operator">&lt;&lt;</span> shift <span class="token comment">// 低 5 位是数据</span>
    shift <span class="token operator">+=</span> <span class="token number">5</span>
  <span class="token punctuation">}</span> <span class="token keyword">while</span> <span class="token punctuation">(</span>digit <span class="token operator">&amp;</span> <span class="token number">32</span><span class="token punctuation">)</span> <span class="token comment">// 第 6 位是「后面还有」</span>
  <span class="token keyword">const</span> negative <span class="token operator">=</span> result <span class="token operator">&amp;</span> <span class="token number">1</span> <span class="token comment">// 最低位是符号</span>
  result <span class="token operator">=</span> result <span class="token operator">&gt;&gt;&gt;</span> <span class="token number">1</span>
  <span class="token keyword">return</span> <span class="token punctuation">[</span>negative <span class="token operator">?</span> <span class="token operator">-</span>result <span class="token operator">:</span> result<span class="token punctuation">,</span> i<span class="token punctuation">]</span>
<span class="token punctuation">}</span>

<span class="token keyword">function</span> <span class="token function">decodeMappings</span><span class="token punctuation">(</span><span class="token parameter">mappings</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
  <span class="token keyword">let</span> sourceIndex <span class="token operator">=</span> <span class="token number">0</span>
  <span class="token keyword">let</span> sourceLine <span class="token operator">=</span> <span class="token number">0</span>
  <span class="token keyword">let</span> sourceColumn <span class="token operator">=</span> <span class="token number">0</span>
  <span class="token keyword">let</span> nameIndex <span class="token operator">=</span> <span class="token number">0</span>

  <span class="token keyword">return</span> mappings<span class="token punctuation">.</span><span class="token function">split</span><span class="token punctuation">(</span><span class="token string">';'</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">flatMap</span><span class="token punctuation">(</span><span class="token punctuation">(</span><span class="token parameter">line<span class="token punctuation">,</span> genLine</span><span class="token punctuation">)</span> <span class="token operator">=&gt;</span> <span class="token punctuation">{</span>
    <span class="token keyword">let</span> genColumn <span class="token operator">=</span> <span class="token number">0</span> <span class="token comment">// 换行时列归零</span>
    <span class="token keyword">return</span> line
      <span class="token punctuation">.</span><span class="token function">split</span><span class="token punctuation">(</span><span class="token string">','</span><span class="token punctuation">)</span>
      <span class="token punctuation">.</span><span class="token function">filter</span><span class="token punctuation">(</span>Boolean<span class="token punctuation">)</span>
      <span class="token punctuation">.</span><span class="token function">map</span><span class="token punctuation">(</span><span class="token punctuation">(</span><span class="token parameter">segment</span><span class="token punctuation">)</span> <span class="token operator">=&gt;</span> <span class="token punctuation">{</span>
        <span class="token keyword">let</span> i <span class="token operator">=</span> <span class="token number">0</span>
        <span class="token keyword">let</span> d
        <span class="token punctuation">;</span><span class="token punctuation">[</span>d<span class="token punctuation">,</span> i<span class="token punctuation">]</span> <span class="token operator">=</span> <span class="token function">decodeVLQ</span><span class="token punctuation">(</span>segment<span class="token punctuation">,</span> i<span class="token punctuation">)</span>
        genColumn <span class="token operator">+=</span> d
        <span class="token punctuation">;</span><span class="token punctuation">[</span>d<span class="token punctuation">,</span> i<span class="token punctuation">]</span> <span class="token operator">=</span> <span class="token function">decodeVLQ</span><span class="token punctuation">(</span>segment<span class="token punctuation">,</span> i<span class="token punctuation">)</span>
        sourceIndex <span class="token operator">+=</span> d
        <span class="token punctuation">;</span><span class="token punctuation">[</span>d<span class="token punctuation">,</span> i<span class="token punctuation">]</span> <span class="token operator">=</span> <span class="token function">decodeVLQ</span><span class="token punctuation">(</span>segment<span class="token punctuation">,</span> i<span class="token punctuation">)</span>
        sourceLine <span class="token operator">+=</span> d
        <span class="token punctuation">;</span><span class="token punctuation">[</span>d<span class="token punctuation">,</span> i<span class="token punctuation">]</span> <span class="token operator">=</span> <span class="token function">decodeVLQ</span><span class="token punctuation">(</span>segment<span class="token punctuation">,</span> i<span class="token punctuation">)</span>
        sourceColumn <span class="token operator">+=</span> d
        <span class="token keyword">if</span> <span class="token punctuation">(</span>i <span class="token operator">&lt;</span> segment<span class="token punctuation">.</span>length<span class="token punctuation">)</span> <span class="token punctuation">{</span>
          <span class="token punctuation">;</span><span class="token punctuation">[</span>d<span class="token punctuation">,</span> i<span class="token punctuation">]</span> <span class="token operator">=</span> <span class="token function">decodeVLQ</span><span class="token punctuation">(</span>segment<span class="token punctuation">,</span> i<span class="token punctuation">)</span>
          nameIndex <span class="token operator">+=</span> d
        <span class="token punctuation">}</span>
        <span class="token keyword">return</span> <span class="token punctuation">{</span> <span class="token literal-property property">genLine</span><span class="token operator">:</span> genLine <span class="token operator">+</span> <span class="token number">1</span><span class="token punctuation">,</span> genColumn<span class="token punctuation">,</span> <span class="token literal-property property">sourceLine</span><span class="token operator">:</span> sourceLine <span class="token operator">+</span> <span class="token number">1</span><span class="token punctuation">,</span> sourceColumn<span class="token punctuation">,</span> nameIndex <span class="token punctuation">}</span>
      <span class="token punctuation">}</span><span class="token punctuation">)</span>
  <span class="token punctuation">}</span><span class="token punctuation">)</span>
<span class="token punctuation">}</span>
</code></pre></div><p>拿它跑前面那份 map，前几段是：</p> <div class="language- extra-class"><pre class="language-text"><code>gen 1:0   -&gt; src 1:0    name=greet
gen 1:9   -&gt; src 1:9    name=greet
gen 1:15  -&gt; src 1:15   name=name
gen 1:18  -&gt; src 3:2    name=name
</code></pre></div><p>我又拿 <code>source-map</code> 官方库对了一遍，12 段里 11 段完全一致（第 4 段不一致是因为两个映射点落到了同一列，库取了先到的那个）。<strong>这份格式是可以自己读懂的，它并不需要黑箱。</strong></p> <h2 id="工程上真正要操心的三件事"> 工程上真正要操心的三件事</h2> <p>懂了格式，剩下的都是配置问题。</p> <p><img src="https://bougieL.github.io/images/2026/09/22/fig4_001.jpg" alt="一排锁着的档案柜，其中一个抽屉半开着，透出光" loading="lazy"></p> <p><strong>一、要不要生成，生成哪种。</strong></p> <div class="language-js extra-class"><pre class="language-js"><code><span class="token comment">// vite.config.js</span>
<span class="token keyword">export</span> <span class="token keyword">default</span> <span class="token punctuation">{</span>
  <span class="token literal-property property">build</span><span class="token operator">:</span> <span class="token punctuation">{</span>
    <span class="token literal-property property">sourcemap</span><span class="token operator">:</span> <span class="token string">'hidden'</span> <span class="token comment">// true | 'inline' | 'hidden'</span>
  <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre></div><div class="language-js extra-class"><pre class="language-js"><code><span class="token comment">// webpack</span>
module<span class="token punctuation">.</span>exports <span class="token operator">=</span> <span class="token punctuation">{</span>
  <span class="token literal-property property">devtool</span><span class="token operator">:</span> <span class="token string">'hidden-source-map'</span>
<span class="token punctuation">}</span>
</code></pre></div><p>webpack 那串 <code>devtool</code> 值不是随便起的：<code>source-map</code> 会在产物末尾加 <code>//# sourceMappingURL=</code> 注释；<code>hidden-source-map</code> 生成 map 但<strong>不</strong>加注释；<code>nosources-source-map</code> 保留位置映射但去掉 <code>sourcesContent</code>。开发环境的 <code>eval-cheap-module-source-map</code> 则是另一条路：用 <code>eval</code> 换重建速度，只映射行不映射列。</p> <p><strong>二、别把 map 放到公网。</strong></p> <p>这是最容易被忽略的一条。map 里有两样东西不该给外人看：<code>sourcesContent</code>（整份源码）和 <code>sources</code>（有时候是绝对路径，把内网目录结构都带出去了）。</p> <p>正确做法是 <code>hidden-source-map</code>：产物里不留 <code>sourceMappingURL</code> 注释，浏览器不会去找它；然后把 map 上传到监控平台（Sentry、阿里前端监控这些都支持），平台按版本号把线上报错和 map 配对。用户看不到源码，你能看到原始堆栈。</p> <p>如果你连平台都不想给源码，还有 <code>nosources-source-map</code>：位置能还原，内容不给。折中得很体面。</p> <p><strong>三、怎么让浏览器找到它。</strong></p> <p>两种方式，效果一样：</p> <div class="language- extra-class"><pre class="language-text"><code>//# sourceMappingURL=app.a8f3c2.js.map        // 写在产物末尾
SourceMap: /app.a8f3c2.js.map                 // 或者放在 HTTP 响应头里
</code></pre></div><p>用响应头的好处是产物一个字节都不用改。注意跨域的话 map 也要带 CORS 头，否则浏览器会静默放弃，DevTools 里表现为「能打开源文件但内容空白」。</p> <h2 id="那几个让人白掉头发的坑"> 那几个让人白掉头发的坑</h2> <p><strong>位置对不上，八成是版本不一致。</strong> 报错的列号来自 CDN 上的那份 JS，而你手里的 map 是另一次构建的。构建缓存、回滚、CDN 没刷新，任何一个都能造成错位。查这个之前，先确认产物的 hash 和 map 是一对。</p> <p><strong>链式转换会丢信息。</strong> TS → JS → 压缩是两级转换。第二级（压缩器）只认得第一级的产物，如果第一级的 map 没喂给它，最终 map 就会指到中间那层 JS 而不是你的 <code>.ts</code>。tsc 要给 <code>sourceMap: true</code>，bundler 要配置成读取上游 map。</p> <p><strong>异步堆栈的落点是「抛出位置」，不是「调用位置」。</strong> Promise 里抛的错误，列号指向的是那个 <code>throw</code>，而你可能更想知道是谁调进来的。这不是 map 的问题，是 <code>window.onerror</code> 拿到的信息本来就不全——需要在捕获时把 <code>error.stack</code> 一起上报。</p> <p><strong><code>sourcesContent</code> 和「no sources」提示。</strong> DevTools 提示 "no sources" 通常就是 map 里没有 <code>sourcesContent</code>，而源文件又拿不到。不是 map 坏了。</p> <h2 id="结尾"> 结尾</h2> <p>回到那条报警。</p> <p>后来查出来是 <code>src/utils/format.js</code> 第 42 行，一个从接口拿的对象少了个字段。从看到报警到定位，一共七分钟——如果我没配 source map，这七分钟会变成一下午，而且大概率以「先加个 try/catch 看看」收场。</p> <p><img src="https://bougieL.github.io/images/2026/09/22/fig5_001.jpg" alt="夜里一束灯塔的光穿过浓雾照在漆黑的水面上" loading="lazy"></p> <p><code>38247</code> 这个数字本身没有任何意义。它有意义，是因为存在一份文件，愿意记下「这个位置原来在哪」。</p> <p>这份格式 2011 年定稿，之后几乎没变过。十几年里前端换了三轮框架、构建工具换了两代，它安安静静躺在产物旁边，被下载、被解析、被对上一个列号，然后被忘掉。</p> <p>我觉得大部分基础设施的好，都是这种好：<strong>你从来不需要想起它，只在它缺席的那天，才发现自己寸步难行。</strong></p> <hr> <p>文中的示例由 esbuild 与 terser 生成，解码结果已与 <code>source-map</code> 官方库对照验证。</p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[拔掉网线之后：本地优先软件与 CRDT]]></title>
            <description><![CDATA[在高铁隧道里改了一份文档，第二天发现它变成了两个「冲突副本」。这件小事让我认真想了一次：为什么我们默认数据存在别人的机器上？以及，要让「本地才是主副本」这件事真正成立，工程上得付出什么代价。]]></description>
            <link>https://bougieL.github.io/2026/09/19/local-first-crdt/</link>
            <guid isPermaLink="true">https://bougieL.github.io/2026/09/19/local-first-crdt/</guid>
            <pubDate>Sat, 19 Sep 2026 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<p>上周在高铁上，我改一份方案。</p> <p>隧道里没有信号。那个笔记应用没有转圈，没有弹「网络不可用」，我能继续打字，甚至还能看见光标。我当时还挺感动，觉得它做得挺体面。</p> <p>出了隧道我合上电脑。第二天打开，那份文档旁边多了一个文件：<code>方案 (冲突副本 2026-09-11)</code>。</p> <p>我盯着那个文件名看了一会儿。它不是报错，也没有丢东西——两个版本都在，我自己拼回去就行。但这个结果让我愣了一下：我在隧道里那二十分钟以为自己在「编辑」，其实只是在「暂存」。</p> <p><img src="https://bougieL.github.io/images/2026/09/19/cover.jpeg" alt="一根网线的特写，水晶头和线缆" loading="lazy"></p> <h2 id="云优先这个默认-其实挺奇怪的"> 云优先这个默认，其实挺奇怪的</h2> <p>我们这代人已经很习惯一件事：文件存在别人的机器上，自己硬盘上的那份只是缓存。</p> <p>顺着这个默认往下推，会推出一些不太对劲的结论：</p> <ul><li>你想看三年前写的东西，得先联网。那东西是你的，但你打不开它；</li> <li>公司关停服务，你的数据就没了。这不是假设，Google Reader、Delicious、无数个 sync 服务都发生过；</li> <li>本地明明有这份文件，「打开」这个动作却要等一次网络往返；</li> <li>最微妙的是心理层面的：你不再觉得它是「你的文件」，你觉得它是「你的账号里的文件」。</li></ul> <p>2019 年 Ink &amp; Switch 那篇《Local-First Software》把反过来做的那套东西给了个名字。它的核心主张只有一句：<strong>数据的主副本在你本地，网络只负责同步，不负责授权。</strong></p> <p><img src="https://bougieL.github.io/images/2026/09/19/fig-1.jpeg" alt="一排服务器机柜，指示灯亮着" loading="lazy"></p> <p>论文里列了七条理想，我最在意的是第三条：网络是可选的（The network is optional）。注意它不是说「要支持离线模式」，而是说——在线只是同步的时机，不是工作的前提。</p> <p>这句话听着简单，实现起来是另一回事。因为一旦主副本在本地，你就躲不开那个问题了。</p> <h2 id="同步为什么难"> 同步为什么难</h2> <p>如果只有你一个人，同步根本不叫问题：把文件传上去，完事。</p> <p>一旦有两个人同时改，你就必须回答一个问题：<strong>谁的改动算数？</strong></p> <p>最朴素的答案是「最后写入者获胜」（Last-Write-Wins，LWW）。给每次改动盖一个时间戳，谁的晚，谁留下。</p> <p><img src="https://bougieL.github.io/images/2026/09/19/fig-2.jpeg" alt="一条岔路，路牌指向不同方向" loading="lazy"></p> <p>它有两个大坑。</p> <p>第一个坑是时钟。设备时钟会漂移，会跨时区，会有人手动调过，NTP 也不总能救你。「谁更晚」这个判断，在分布式系统里比你想象的不可靠得多。</p> <p>第二个坑更严重：LWW 的本质是<strong>丢弃</strong>，不是合并。我改了标题，你改了正文，这两件事本来可以共存，但 LWW 只留一个——它不知道「标题」和「正文」是两个字段，它只知道「这份文档在 15:32:07 被写了一次」。</p> <p><img src="https://bougieL.github.io/images/2026/09/19/fig-3.jpeg" alt="一只沙漏，沙子正在往下漏" loading="lazy"></p> <p>真正让人崩溃的场景也在这：我只改了一个字，结果我的整份文档被覆盖了。从用户视角看，这不叫冲突，这叫丢数据。</p> <p>另一条朴素的路是加锁：谁在编辑谁锁住，别人只能看。Office 共享文档和早期 Google Docs 都这么干过。它好懂，但它把「协作」变成了「排队」，而且锁的释放本身就不可靠——客户端崩了怎么办？</p> <h2 id="两条路-ot-和-crdt"> 两条路：OT 和 CRDT</h2> <p>工业界真正跑通的是另外两条路。</p> <p>第一条叫 <strong>OT（Operational Transformation，操作变换）</strong>。它把改动描述成「操作」——不是「文档变成了什么」，而是「在第 5 个字符后面插入 abc」。两个人并发改，服务端收到两个操作，把它们互相「变换」一下：让后到的那个适配先到的那个已经造成的效果，再应用。</p> <p>Google Docs 用的就是这套。它很成熟，文档结构也紧凑。代价是它需要一个中心服务器来定全序——每个操作都得由服务端发一个序号。一旦服务端是唯一的裁判，「本地是主副本」就没法彻底成立：离线期间你只能攒着操作本地重放，重连时再让服务端帮你变换，而这里面的边缘情况多到能写一本书。</p> <p>第二条路叫 <strong>CRDT（Conflict-free Replicated Data Type）</strong>。它换了个思路：不去「变换」冲突，而是<strong>把数据结构本身设计成——不管以什么顺序合并，结果都一样。</strong></p> <p><img src="https://bougieL.github.io/images/2026/09/19/fig-4.jpeg" alt="一盘棋局，黑白格子交错" loading="lazy"></p> <p>具体就是三个性质：</p> <ul><li><strong>交换律</strong>：<code>merge(a, b)</code> 和 <code>merge(b, a)</code> 结果相同；</li> <li><strong>结合律</strong>：<code>merge(merge(a, b), c)</code> 和 <code>merge(a, merge(b, c))</code> 结果相同；</li> <li><strong>幂等</strong>：<code>merge(a, a)</code> 还是 <code>a</code>。</li></ul> <p>只要这三条成立，你就可以随便合并——任意顺序、任意次数、重复合并——最终一定收敛到同一个状态。不需要中心服务器，不需要全局定序，不需要锁。冲突不是被「解决」的，是<strong>根本不发生</strong>。</p> <p>这三行听起来像废话，但把它们落到具体数据结构上，是过去十几年分布式系统里最漂亮的一段工作。</p> <p>举几个例子。</p> <p><strong>只增计数器（G-Counter）</strong>：每个副本有一份数组，自己那个槽位 +1，合并时逐位取最大值。计数只会涨不会丢，两个人同时点赞一定都算上。</p> <p><strong>集合（OR-Set）</strong>：加元素时给它一个唯一 tag；删除不是真删，而是把这个 tag 扔进一个「墓碑」集合。合并就是并集。这样「我加了、你删了」这类恼人的情况就有了确定答案。</p> <p><strong>文本</strong>，这是最难的一个。难点在于「在第 5 个字符后面插入」这句话，在别人也在插入的时候会漂移——你说的「第 5 个」，和他说的「第 5 个」，可能不是同一个位置。</p> <p>做法是给每个字符发一个全局唯一、且能定序的 ID（fractional indexing，或者 Lamport 时间戳加站点 ID）。删除同样不是真删，是立个墓碑。这样每个字符的位置由它的 ID 决定，不受别人插入的影响。Yjs 的 YATA、Automerge 的 RGA，都是这一类结构的不同实现。</p> <p>落到代码里，大概是这样：</p> <div class="language-js extra-class"><pre class="language-js"><code><span class="token keyword">import</span> <span class="token operator">*</span> <span class="token keyword">as</span> <span class="token constant">Y</span> <span class="token keyword">from</span> <span class="token string">'yjs'</span>

<span class="token keyword">const</span> doc <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">Y<span class="token punctuation">.</span>Doc</span><span class="token punctuation">(</span><span class="token punctuation">)</span>
<span class="token keyword">const</span> text <span class="token operator">=</span> doc<span class="token punctuation">.</span><span class="token function">getText</span><span class="token punctuation">(</span><span class="token string">'content'</span><span class="token punctuation">)</span>

text<span class="token punctuation">.</span><span class="token function">insert</span><span class="token punctuation">(</span><span class="token number">0</span><span class="token punctuation">,</span> <span class="token string">'你好'</span><span class="token punctuation">)</span>

<span class="token comment">// 把状态序列化成一段二进制，可以存 IndexedDB，也可以发给别人</span>
<span class="token keyword">const</span> update <span class="token operator">=</span> <span class="token constant">Y</span><span class="token punctuation">.</span><span class="token function">encodeStateAsUpdate</span><span class="token punctuation">(</span>doc<span class="token punctuation">)</span>

<span class="token comment">// 另一台机器，可能是三天后，可能是离线状态下</span>
<span class="token keyword">const</span> doc2 <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">Y<span class="token punctuation">.</span>Doc</span><span class="token punctuation">(</span><span class="token punctuation">)</span>
<span class="token constant">Y</span><span class="token punctuation">.</span><span class="token function">applyUpdate</span><span class="token punctuation">(</span>doc2<span class="token punctuation">,</span> update<span class="token punctuation">)</span>

doc2<span class="token punctuation">.</span><span class="token function">getText</span><span class="token punctuation">(</span><span class="token string">'content'</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">toString</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token comment">// '你好'</span>
</code></pre></div><p>这段代码看着平平无奇，重点是 <code>applyUpdate</code> 可以<strong>乱序调用、重复调用</strong>，结果都一样。你不用关心这段 update 是第几个到的、有没有到过。这就是交换律和幂等给你的东西——也是「网络是可选的」能成立的原因。</p> <h2 id="但你得为它付钱"> 但你得为它付钱</h2> <p>天下没有免费的收敛。CRDT 的代价非常实在：</p> <p><strong>墓碑不消失。</strong> 删掉的东西还在那儿，只是看不见。一篇被反复修改一万次的文档，内部结构里躺着一万个字符的骨架。Yjs 做了不少优化（合并连续的删除、用整数压缩 ID），但量级问题没有消失。</p> <p><strong>ID 比内容大。</strong> 为了能全局定序，每个字符都要带上站点 ID 和时钟。一篇十万字的文档，内部表示可能是正文的几倍。这个数字在浏览器里是要进内存的。</p> <p><strong>调试像考古。</strong> 你看到的 <code>abc</code>，内部是一串带着 ID 和墓碑的链表。出了 bug，你没有一个「人类可读」的视图可以打开看。这是我见过的每个用 CRDT 的团队都抱怨过的事。</p> <p><strong>「最终」到底有多最终。</strong> CRDT 保证的是最终一致，不是当下一致。两个副本在断网期间各自演化，重连后合并的语义是「都保留」——对文本来说是对的，但对某些东西是错的。比如两个人都把同一个开关关掉了两次，你想要的答案可能是「关一次」，而不是「两次都记下来」。</p> <h2 id="谁在真的用它"> 谁在真的用它</h2> <p>不是纸上谈兵，已经有不少产品跑在上面：</p> <ul><li><strong>Figma</strong>：多人同时在一个画布上操作，它的场景树本质上就是一套 CRDT 化的结构；</li> <li><strong>Linear</strong>：任务状态同步，断网可用，重连后自动合并；</li> <li><strong>苹果的备忘录和提醒事项</strong>：CloudKit 底层用的就是 CRDT，Apple 在工程博客里公开讲过；</li> <li><strong>Obsidian Sync 和一批本地 markdown 编辑器</strong>：文件本身就是本地主副本，同步只是同步；</li> <li>前端最主流的两个库是 <strong>Yjs</strong> 和 <strong>Automerge</strong>，前者性能更好，后者历史更久、语义更讲究。</li></ul> <p>顺带说一句 git。git 其实是个「人工 CRDT」：它的 DAG 保证任意顺序合并同一组提交，结果一致；真正冲突的地方它不猜，原样停下来交给你。</p> <p>差别在于粒度：git 的合并单位是文件行，而且它<strong>愿意停下来问你</strong>。CRDT 不愿意停下来——这是它的优点，也是它最贵的代价。</p> <h2 id="结尾"> 结尾</h2> <p>回到那趟高铁。</p> <p>我现在的做法很土：文档是本地的 markdown 文件，同步靠 git。它一点都不优雅，冲突时照样要我手动解。</p> <p>但它有一件 CRDT 给不了我的东西：<strong>那个冲突我看得见。</strong></p> <p><img src="https://bougieL.github.io/images/2026/09/19/fig-5.jpeg" alt="一台老式打字机，键帽排列整齐" loading="lazy"></p> <p>本地优先这件事，我想说的其实不是「离线也能用」。那只是顺带的好处。</p> <p>真正被换掉的是那个默认假设：数据在我的硬盘上，网络只是让它也出现在别的地方——而不是反过来。</p> <p>CRDT 是把这个假设在工程上变可行的那半边。它不完美，墓碑、膨胀、调试难，每一个都是真问题，随便一个都能劝退人。</p> <p>但它是我见过的第一个不需要「找个中心来裁判」、就能让好几个人同时写同一份东西的办法。这个想法本身，我觉得比任何一个具体的库都值钱。</p> <hr> <p>题图与部分配图来自开放授权的摄影作品，感谢这些作者：Crossroads: Invest or Spend — ccPixs.com；Hourglass — dinohoax；Chess Middlegame Puzzle - 3 — ChessNetwork；typewriter — Ak~i（均为 CC BY 2.0，Flickr）。</p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[浏览器终于拿到了显卡]]></title>
            <description><![CDATA[WebGPU 落地这件事被低估了。浏览器第一次有了正经的 GPU 通用计算通道——不是把数据塞进纹理里骗着色器干活，而是真的能提交一个计算任务。但被它改变的并不是游戏，是另一件几乎没人预料到的事。]]></description>
            <link>https://bougieL.github.io/2026/09/18/browser-finally-got-a-gpu/</link>
            <guid isPermaLink="true">https://bougieL.github.io/2026/09/18/browser-finally-got-a-gpu/</guid>
            <pubDate>Fri, 18 Sep 2026 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<p>上周我打开一个网页，进度条走了二十来秒，然后一个语言模型开始在我这台机器上回答我的问题。</p> <p>没有后端，没有 API Key，没有「服务繁忙请稍后再试」。我关掉 WiFi 它照样在跑——因为我的问题一个字节都没离开过这台电脑。</p> <p>那二十秒里发生的事情是：浏览器下载了几 GB 的权重，把它们塞进显卡显存，然后开始一遍一遍地做矩阵乘法。</p> <p>我对着 DevTools 看了一会儿，心里冒出来的不是「好快」，而是另一句话：等等，浏览器什么时候能干这个了？</p> <p><img src="https://bougieL.github.io/images/2026/09/18/cover.jpeg" alt="一块显卡的特写，散热风扇和电路板" loading="lazy"></p> <h2 id="先回忆一下-webgl-有多拧巴"> 先回忆一下 WebGL 有多拧巴</h2> <p>WebGL 本质上是 OpenGL ES 在浏览器里的一层绑定。它的整个设计目标是：把一个三角形画到屏幕上。</p> <p>它没有计算着色器。一个像素一个像素地做数学，这件事在 WebGL 的世界观里根本不存在。</p> <p>但人是很擅长绕路的。过去十几年，想在浏览器里用 GPU 算点什么的人，都是这么干的：</p> <ol><li>把要算的数组，编码进一张纹理的 RGBA 通道里（一个 float 拆成四个字节，或者祈祷设备支持浮点纹理）；</li> <li>画一个铺满屏幕的矩形，让片元着色器在每个像素上跑一次——这个「像素」其实就是你的数组下标；</li> <li>把结果渲染到一张新的纹理上；</li> <li>用 <code>readPixels</code> 把结果读回 CPU。</li></ol> <div class="language-glsl extra-class"><pre class="language-glsl"><code><span class="token keyword">precision</span> <span class="token keyword">highp</span> <span class="token keyword">float</span><span class="token punctuation">;</span>
<span class="token keyword">uniform</span> <span class="token keyword">sampler2D</span> u_data<span class="token punctuation">;</span>
<span class="token keyword">varying</span> <span class="token keyword">vec2</span> v_uv<span class="token punctuation">;</span>

<span class="token keyword">void</span> <span class="token function">main</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
  <span class="token keyword">vec4</span> v <span class="token operator">=</span> <span class="token function">texture2D</span><span class="token punctuation">(</span>u_data<span class="token punctuation">,</span> v_uv<span class="token punctuation">)</span><span class="token punctuation">;</span>
  gl_FragColor <span class="token operator">=</span> <span class="token keyword">vec4</span><span class="token punctuation">(</span>v<span class="token punctuation">.</span>r <span class="token operator">*</span> <span class="token number">2.0</span><span class="token punctuation">,</span> v<span class="token punctuation">.</span>g <span class="token operator">*</span> <span class="token number">2.0</span><span class="token punctuation">,</span> v<span class="token punctuation">.</span>b <span class="token operator">*</span> <span class="token number">2.0</span><span class="token punctuation">,</span> v<span class="token punctuation">.</span>a<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre></div><p>这段代码做的事是「把一个数组每个元素乘 2」。它伪装成了一次绘制。</p> <p>这个套路能用，社区里也真有人靠它做出了像样的东西。但它从头到脚都在拧巴：</p> <ul><li>你只能「按输出位置写」，不能任意下标写入。想做排序、想做稀疏更新，就得重新设计成一次「绘制」；</li> <li>精度是借来的。8 bit 一个通道，浮点纹理要看扩展脸色；</li> <li>中间结果不能留在显存里反复用，每一趟都得走一遍「渲染到纹理」的仪式；</li> <li><code>readPixels</code> 是一次同步回读，会把整条流水线按住等你；</li> <li>最要命的是报错。WebGL 的经典调试体验是：你写错了，画面全黑，<code>getError()</code> 返回 0，浏览器一句话不说。</li></ul> <p><img src="https://bougieL.github.io/images/2026/09/18/fig-1.jpeg" alt="一张面具，挂在暗处" loading="lazy"></p> <p>我写过的那段 shader 出了 bug，我调了一个下午。最后发现是纹理坐标差了半个像素。</p> <p>不是我笨。是那套 API 从头到尾都在要求我扮演一个画家，而我其实只是想算个数。</p> <h2 id="webgpu-换掉的其实不是性能"> WebGPU 换掉的其实不是性能</h2> <p>很容易把 WebGPU 理解成「WebGL 3，更快」。这个理解错得有点可惜。</p> <p>WebGPU 不是 WebGL 的下一代，它是 Vulkan、Metal、D3D12 这一代原生图形 API 在浏览器里的一层薄封装。它和 WebGL 的关系，比较接近「重新设计的另一套东西」，而不是升级包。</p> <p>真正的变化是这几条：</p> <p><strong>计算任务不再需要伪装。</strong> 你可以直接创建一条计算管线，指定工作组大小，提交一个 dispatch。没有矩形，没有纹理，没有 <code>gl_FragColor</code>。你想让 GPU 算什么，就直说。</p> <p><strong>数据终于可以是数组了。</strong> <code>storage buffer</code> 是一块真正的、可读可写的显存。不是把 float 拆成四个字节藏进 RGBA，是 <code>array&lt;f32&gt;</code>，能按下标读写，能在多个 pass 之间留在显存里不来回搬。</p> <p><strong>状态不用每帧重设一遍。</strong> WebGL 是一台巨大的全局状态机，每次 draw call 之前你得把一堆开关拨到位。WebGPU 把管线、绑定组、资源布局预先创建好，运行时每帧基本只剩「录制命令 + 提交」两件事。这也是它在 draw call 极多的场景里 CPU 开销低得多的原因。</p> <p><strong>错误在创建时就报。</strong> 参数不对、格式不匹配、越界访问，大部分在你创建管线或绑定组的那一刻就被校验拦下，并且给你一句人能读的话。从「画面全黑默不作声」到「创建时就告诉你哪里错了」，这个体验差异被严重低估了。</p> <p><img src="https://bougieL.github.io/images/2026/09/18/fig-2.jpeg" alt="芯片电路板的微距照片" loading="lazy"></p> <h2 id="wgsl-一门没人主动想学的语言"> WGSL：一门没人主动想学的语言</h2> <p>代价在这儿。</p> <p>WebGPU 没有沿用 GLSL，而是配了一门新语言，WGSL。</p> <div class="language-wgsl extra-class"><pre class="language-text"><code>@group(0) @binding(0) var&lt;storage, read&gt; a: array&lt;f32&gt;;
@group(0) @binding(1) var&lt;storage, read&gt; b: array&lt;f32&gt;;
@group(0) @binding(2) var&lt;storage, read_write&gt; out: array&lt;f32&gt;;

@compute @workgroup_size(64)
fn main(@builtin(global_invocation_id) gid: vec3&lt;u32&gt;) {
  let i = gid.x;
  if (i &gt;= arrayLength(&amp;out)) {
    return;
  }
  out[i] = a[i] + b[i];
}
</code></pre></div><p>语法一眼看上去像 Rust，但它不是 Rust。类型声明严格，隐式转换极少，<code>vec3&lt;u32&gt;</code> 和 <code>vec3&lt;f32&gt;</code> 是两回事，写错了编译器会追着你说。</p> <p>为什么不用现成的 GLSL？理由其实很硬：GLSL 的各个方言差异太大，而浏览器要的是「同一份着色器在三种原生 API 上都能编译，并且行为一致」。于是干脆重新定义一门语义收敛、可静态验证的语言。顺带还能把越界访问这类问题变成「有定义的行为」，而不是原生世界里那种看驱动的未定义行为。</p> <p>这些理由都对。但不影响一个事实：要吃这顿饭，你得先学一门新语言，外加一套 GPU 内存模型。</p> <p>这不是装个 npm 包看两眼文档就能上手的东西。</p> <p><img src="https://bougieL.github.io/images/2026/09/18/fig-3.jpeg" alt="屏幕上是一行行代码，键盘在下方" loading="lazy"></p> <h2 id="三家浏览器都到齐了"> 三家浏览器都到齐了</h2> <p>可用性这条线，走了挺久。</p> <p>Chrome 在 2023 年就在桌面端默认打开了 WebGPU。Firefox 和 Safari 则在 2025 年先后跟上，补齐了最后两块。</p> <p>到今天，作为前端谈「要不要用 WebGPU」，第一次变成了一个纯技术判断，而不是一场兼容性赌博。</p> <p>这件事的意义比听起来大。过去十年里，浏览器上有大量技术不是死在技术上，而是死在「Safari 不支持」上。WebGPU 花了这么多年，把所有平台都熬到了同一张桌子上。</p> <h2 id="但被它改变的不是游戏"> 但被它改变的不是游戏</h2> <p>WebGPU 立项的时候，所有人脑子里想的都是同一件事：3A 游戏要进浏览器了。</p> <p>这事没发生。至少到目前为止没有，而且我怀疑短期内也不会。</p> <p>真正被改变的是另一件几乎没人预料到的事：<strong>模型推理跑到了浏览器里。</strong></p> <p>Transformer 的核心计算是大规模矩阵乘法——恰好是 GPU 最擅长、也恰好是 WebGL 最别扭的那种任务。WebGPU 的 storage buffer 和计算着色器一到位，这条路突然就通了：</p> <ul><li>Hugging Face 的 transformers.js 加了 WebGPU 后端；</li> <li>MLC 的 WebLLM 直接在浏览器里跑起了对话模型；</li> <li>ONNX Runtime Web 也提供了 WebGPU 执行提供程序。</li></ul> <p>我开头说的那个网页，就是这一类东西。</p> <p>它带来的不只是「能跑」，而是三个很少同时出现的东西凑到了一起：</p> <p><strong>数据不出机器。</strong> 这不是营销话术。对于一些不能上传到第三方的场景——病历、合同、内部文档——本地推理几乎是目前唯一体面的解法。</p> <p><strong>算力是用户的。</strong> 服务端推理的成本是按 token 付的，而且是持续付。浏览器推理的成本是用户电费，一次性。对一个访问量不小但单次价值不高的小工具来说，这个账差别巨大。</p> <p><strong>分发是一个 URL。</strong> 不用装，不用注册，不用配环境，点开就能用。原生应用在这件事上永远追不上网页。</p> <p><img src="https://bougieL.github.io/images/2026/09/18/fig-4.jpeg" alt="发光的神经网络连接，像一团缠绕的光丝" loading="lazy"></p> <p>顺带被带起来的还有实时视频处理（滤镜、抠像、超分）和大规模散点可视化——几十万个点这种数量级，交给 GPU 之后从「卡死」变成「流畅」。</p> <p>这些场景以前也不是完全不能做，只是要么慢到不可用，要么得让用户先装一个桌面软件。</p> <h2 id="泼点冷水"> 泼点冷水</h2> <p>说了这么多好处，我得把另一半也说完。</p> <p><strong>设备差异没有消失，只是换了个位置。</strong> 手机 GPU、老显卡、Linux 上的开源驱动，各有各的脾气。WebGPU 收敛了 API，收敛不了硬件。你写的着色器在 M3 上跑得很好，在某一年的集显上可能直接超限。</p> <p><strong>显存是真有上限的。</strong> 而且浏览器为了安全和稳定，给的限额通常比原生更保守。大模型在浏览器里能跑，代价是你要精心地量化、分片、管理生命周期。</p> <p><strong>调试工具还差得远。</strong> 原生生态有成熟的帧捕获和着色器调试工具，WebGPU 这边基本还在「打印中间结果到纹理然后读回来看」的阶段。</p> <p><strong>首帧慢。</strong> 着色器编译、管线创建、权重上传，这些都得算在用户体验里。那个二十秒的进度条不是偶然。</p> <p><strong>以及最实在的一个问题：你真的需要吗？</strong></p> <p>绝大多数的前端场景，Canvas 2D 就够了，连 WebGL 都用不上。为了一个图表去学 WGSL 和 GPU 内存模型，是典型的拿锤子找钉子。</p> <p><img src="https://bougieL.github.io/images/2026/09/18/fig-5.jpeg" alt="一扇开着的门，外面是光" loading="lazy"></p> <h2 id="我的判断"> 我的判断</h2> <p>我不认为 WebGPU 会变成「前端都要学」的东西。它的学习曲线太陡，适用场景太窄，和大多数人的日常工作没关系。</p> <p>我觉得它更像是给那 5% 的场景准备的一张底牌。</p> <p>而这 5% 以前是什么处境？——要么做得很难看，要么只能跪下来求用户装一个原生应用。</p> <p>WebGPU 没有让浏览器变成游戏机，它让浏览器少了一个「做不到」。</p> <p>那天我把那个 tab 关掉之后想了一会儿。二十秒的等待不算愉快，模型的能力也就那样，老实说实用性有限。</p> <p>但「一个网页能吃掉整块显卡」这件事本身，把浏览器的边界往外推了一格。</p> <p>上一次它有这种感觉，还是 WebAssembly 出来的时候。</p> <hr> <p>题图与部分配图来自开放授权的摄影作品，感谢这些作者：Mask — Kríttik'l Kápchər (CH)；electronic circuit board — Creativity103；Coffee code — Silus；Brains on Chains — Shawn DeWolfe；New Orleans Doorway — anthony arrigo。（均为 CC BY 2.0，Flickr）</p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[我数了一遍 node_modules，然后沉默了]]></title>
            <description><![CDATA[我的 package.json 里只写了 32 个依赖，node_modules 里躺着 1140 个包、47772 个文件、571MB。顺手数了下代码行数——我写的和别人写的，比例是 1 比 102。这些数字不吓人，吓人的是我答不出里面有什么。]]></description>
            <link>https://bougieL.github.io/2026/09/17/counting-node-modules/</link>
            <guid isPermaLink="true">https://bougieL.github.io/2026/09/17/counting-node-modules/</guid>
            <pubDate>Thu, 17 Sep 2026 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<p>上周末我想把一个报错查清楚，一路追进了 <code>node_modules</code>。问题最后没查出来，但我顺手敲了个命令：</p> <div class="language- extra-class"><pre class="language-text"><code>$ du -sh node_modules src
571M    node_modules
281M    src
</code></pre></div><p>盯着这两个数字看了好一会儿。</p> <p><code>src</code> 里装的是这个博客从 2018 年到现在的所有东西——文章、配置、自己改的 VuePress 主题，八年的积累。<code>node_modules</code> 里装的是我为了把那些文章变成静态页面而装的东西。后者是前者的两倍。</p> <p>然后我又手贱多敲了几个。</p> <p><img src="https://bougieL.github.io/images/2026/09/17/fig-1.jpeg" alt="望不到头的仓库货架，一个人走在中间" loading="lazy"></p> <h2 id="数字摊开是这样"> 数字摊开是这样</h2> <table><thead><tr><th>项目</th> <th>数字</th></tr></thead> <tbody><tr><td>package.json 里的直接依赖</td> <td>32 个（5 生产 + 27 开发）</td></tr> <tr><td>node_modules 里的包</td> <td>1140 个</td></tr> <tr><td>文件总数</td> <td>47772 个</td></tr> <tr><td>磁盘占用</td> <td>571 MB</td></tr> <tr><td>依赖图最深</td> <td>8 层</td></tr></tbody></table> <p>32 个名字进去，1140 个包出来。</p> <p>我从这 32 个出发写了个 BFS，看看到底连着多少个包：能走到的有 531 个，最深的一条链 8 层。也就是说，我装了 A，A 装了 B，B 装了 C……一直到第八层那个包，我连名字都没见过，但它在我的硬盘上，也会跑在我的构建里。</p> <p>其中最让我笑不出来的是这个：</p> <div class="language- extra-class"><pre class="language-text"><code>$ ls node_modules | grep -c '^is-'
46
</code></pre></div><p>46 个包的名字以 <code>is-</code> 开头。<code>isarray</code> 的全部实现是 5 行。</p> <h2 id="_1-比-102"> 1 比 102</h2> <p>真正让我沉默的是行数。</p> <div class="language- extra-class"><pre class="language-text"><code>$ find src -type f \( -name '*.md' -o -name '*.js' -o -name '*.vue' \) | xargs cat | wc -l
34041

$ find node_modules -type f \( -name '*.js' -o -name '*.ts' \) | xargs cat | wc -l
3480965
</code></pre></div><p>三万四对三百四十八万。102 倍。</p> <p>而且这个算法已经很给我面子了——那 34041 行里绝大部分是 Markdown，是我写的文章，不是代码。真正算得上「我写的代码」的，可能只有几千行。</p> <p>也就是说，这个博客能跑起来，靠的是几千行我写的、和三百多万行我一个字都没读过的。</p> <p><img src="https://bougieL.github.io/images/2026/09/17/fig-2.jpeg" alt="一个小小的人站在一堵由无数小方块垒成的高墙前面" loading="lazy"></p> <h2 id="先别急着吓自己"> 先别急着吓自己</h2> <p>写到这里我得诚实地拐个弯：这个比例本身没什么可吓人的。</p> <p>现代软件本来就是这么造的。没有人从冶炼矿石开始造汽车，你按下启动键的时候发动机里发生的事你一无所知，照样开了十年车。抽象层存在的全部意义，就是让你不必知道下面那一层是什么。</p> <p>那 102 倍不是病态，是分工。它甚至是好事——正是因为我不用自己写解析器、写压缩算法、写 Markdown 的边界处理，我才能花一个下午写一篇博客而不是花半年造工具。</p> <p>真正的问题不在「多」，在「不知道」。</p> <p>我可以接受项目里有三百多万行别人的代码。我接受不了的是我完全说不出里面有什么。要是现在有人问我一句「你的依赖里有没有包会读取环境变量往外发」，我答不上来。这不是假设性的问题，left-pad 之后它就是真的。</p> <h2 id="那-5-行到底配不配单独发个包"> 那 5 行到底配不配单独发个包</h2> <p>关于小包，社区里有个流传很久的嘲笑：连 <code>isarray</code> 都要发个 npm 包，前端真是完了。</p> <p>我一直觉得这个嘲笑搞错了重点。</p> <div class="language-js extra-class"><pre class="language-js"><code><span class="token keyword">var</span> toString <span class="token operator">=</span> <span class="token punctuation">{</span><span class="token punctuation">}</span><span class="token punctuation">.</span>toString<span class="token punctuation">;</span>

module<span class="token punctuation">.</span>exports <span class="token operator">=</span> Array<span class="token punctuation">.</span>isArray <span class="token operator">||</span> <span class="token keyword">function</span> <span class="token punctuation">(</span><span class="token parameter">arr</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
  <span class="token keyword">return</span> <span class="token function">toString</span><span class="token punctuation">.</span><span class="token function">call</span><span class="token punctuation">(</span>arr<span class="token punctuation">)</span> <span class="token operator">==</span> <span class="token string">'[object Array]'</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span><span class="token punctuation">;</span>
</code></pre></div><p>5 行，但它解决的是一个真实存在过、而且会咬人的问题：ES5 之前没有 <code>Array.isArray</code>，从 iframe 里传出来的数组用 <code>instanceof</code> 判断会失效。那 5 行里打包着一个具体的兼容性判断，不是作者懒得想。</p> <p>更要紧的是，如果它不独立成包，这 5 行就会复制进几百个库里，每个库各自维护一份，其中一部分还是写错的。抽出来之后，bug 只需要修一次，修完所有人一起受益。</p> <p>小包不是偷懒，是把同一个坑收敛到一个地方。</p> <p>left-pad 事件经常被当成「小包危险」的证据，但我读出来的意思正好相反：一个 11 行的包被作者下架，半个互联网挂了——这恰恰说明它被验证过、被广泛信任。出事的不是那 11 行，是「一个维护者可以单方面让半个互联网瘫痪」这个结构。</p> <p><img src="https://bougieL.github.io/images/2026/09/17/fig-3.jpeg" alt="水面上一小角，水下是一整座冰山" loading="lazy"></p> <h2 id="噪音也是真的"> 噪音也是真的</h2> <p>话说回来，我不打算把这事全说成好事。</p> <p>那 1140 个包里有相当一部分是纯噪音。同一个功能被三个库各自实现一遍，A 要的版本和 B 要的版本对不上，npm 就老实给你装三份。<code>du</code> 排第一的是 <code>@vuepress</code>，29M，合理。排第二第三的是 <code>leancloud-storage</code> 和 <code>leancloud-realtime</code>，加起来快 50M——它们是我为了右下角那个能留言的小框（valine）拖进来的。</p> <p>为了一个留言框，50M。</p> <p>这个账要认。不是每个包都配得上它的体积，也不是每个依赖都是深思熟虑的结果。有相当一部分只是三年前某个下午我 <code>npm install</code> 了一下，然后就忘了。它们不是「架构决策」，是「当时手快」。</p> <h2 id="ai-让这条曲线更陡"> AI 让这条曲线更陡</h2> <p>最后说点跟现在有关的。</p> <p>这 1140 个包，好歹是别人写的、被别人在 production 里跑过、有 issue 列表、有版本号的东西。某种意义上它们比我自己写的代码更可靠，至少经过检验。</p> <p>但现在我的项目里开始出现另一类代码：我让 AI 写的，我大致扫了一眼觉得对，就提交了。这类代码没有 issue 列表，没被任何人用过，唯一的验证是我那大致一眼。</p> <p>依赖的风险，本质上是「代码量增长的速度超过了理解它的速度」。以前这个缺口好歹有一部分被下载量、star 数和 issue 记录填着——那是陌生人替我做过的检验。现在这块补丁没了。生成一千行的成本几乎降到了零，读完一千行的成本一点没变。</p> <p><img src="https://bougieL.github.io/images/2026/09/17/fig-4.jpeg" alt="传送带把箱子不断送进一条黑暗的隧道" loading="lazy"></p> <h2 id="我什么都没删"> 我什么都没删</h2> <p>沉默完之后，我一个依赖都没删。</p> <p>那 571MB 还躺在那儿，<code>isarray</code> 的 5 行也还在。冷静想想，删掉它们的收益（省几百 MB 磁盘）远小于风险（某个角落的插件突然不认这个版本）。我不是不知道里面有什么，我是懒得知道，而这两件事在短期内后果一样。</p> <p>变的只有一点：我不再把「这不是我写的代码」当成一句免责声明。</p> <p>以前线上出事，如果最后查出来是某个依赖的 bug，我会有点如释重负——不是我的锅。现在我觉得这句话是错的。是我把它装进来的，我享受了它省下的时间，就得认下它带来的不确定性。那 102 倍的三百多万行，是我签字画押借来的。</p> <p>下次 <code>npm install</code> 之前，大概率还是会照装。</p> <p>但至少我知道自己在装什么了。</p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[工具链的第二次出生]]></title>
            <description><![CDATA[升级 Vite 8 那天，构建从 47 秒变成 6 秒，我第一反应是去查缓存是不是没清。这一年前端工具链集体换语言重写了一遍——Rolldown、Oxc、Biome、Rspack，连 TypeScript 自己都用 Go 重写了。但微软为什么选 Go 而不是 Rust？答案跟性能其实没什么关系。]]></description>
            <link>https://bougieL.github.io/2026/09/16/toolchain-second-birth/</link>
            <guid isPermaLink="true">https://bougieL.github.io/2026/09/16/toolchain-second-birth/</guid>
            <pubDate>Wed, 16 Sep 2026 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<p>升级 Vite 8 那天我盯着终端看了好几秒。47 秒变成 6 秒。</p> <p>第一个反应不是高兴，是怀疑。缓存没清吧？插件没加载吧？产物是不是少了什么？我把 <code>dist</code> 目录翻了一遍，该有的都在，文件名里的 hash 也对得上。</p> <p>就是快了。</p> <p>这种不敢相信的感觉挺有意思。我们这行在过去十年里被训练得相信一件事：构建慢是正常的，项目越大越慢是物理规律，你能做的只有加缓存、加机器、把 CI 拆成更多 job。突然有人告诉你这条物理规律不成立，你的第一反应不是接受，是怀疑自己看漏了什么。</p> <p><img src="https://bougieL.github.io/images/2026/09/16/fig-1.jpeg" alt="停下来的传送带旁边放着一只秒表" loading="lazy"></p> <h2 id="这一年到底发生了什么"> 这一年到底发生了什么</h2> <p>如果你最近没盯着工具链，大概低估了这一年的变化幅度。</p> <p>Vite 8 在三月发布，底层的 esbuild + Rollup 双引擎换成了用 Rust 写的 Rolldown。以前开发时 esbuild 负责依赖预打包、生产时 Rollup 负责出包，两套东西对同一份代码的理解一直有细微差别——那些「dev 能跑 build 就挂」的玄学问题，有一半是这么来的。现在只剩一个引擎。</p> <p>TypeScript 7 在七月 GA。编译器核心从 TypeScript 本身移植到了 Go，官方给的数字是 8 到 12 倍。VS Code 那个仓库的类型检查构建从 78 秒掉到 7.5 秒。</p> <p>Oxc 在做解析器、linter、formatter、minifier 的全家桶，Biome 在吃 Prettier 和 ESLint 的份额，Rspack 在接 webpack 的位置。进度各不相同，方向是一致的：原本用 JavaScript 和 TypeScript 写的工具，正在被用系统语言重写一遍。</p> <h2 id="流行的解释漏了最关键的一半"> 流行的解释漏了最关键的一半</h2> <p>关于这件事，社区最常听到的说法是「Rust 快」。</p> <p>这个说法不算错，但它讲的是结果，不是原因。如果只是因为快，为什么是这两年，而不是五年前？esbuild 2020 年就出来了，当年也快得吓人，可主流项目该用 webpack 还是用 webpack。</p> <p>真正变了的其实是另一件事：<strong>终于有人愿意为工具链付账了。</strong></p> <p>写工具的人一直是前端生态里最吃亏的一群。用户几百万，赞助几百块。你要在业余时间维护一个被上百万人依赖的解析器，还得应付每个月新冒出来的语法提案。在这种条件下，一件工具只要「能用」，就没人有动力把它推倒重来——重写意味着几年没有产出，而这几年的收益还不属于你。</p> <p>Vite、Rspack、Oxc 背后都有公司和商业支撑。这不是巧合。</p> <h2 id="微软为什么选了-go"> 微软为什么选了 Go</h2> <p>这是整件事里我最想讲的一段。</p> <p>TypeScript 团队决定做原生移植的时候，几乎所有人都默认他们会用 Rust。毕竟 Rust 是这几年系统语言的新宠，Rolldown、Oxc、Biome、Rspack 清一色 Rust——快、内存安全、没有 GC 停顿。</p> <p>他们选了 Go。</p> <p>官方给的理由朴素得有点扫兴：Go 的编程模型和现有 TypeScript 代码库的结构更接近。TypeScript 编译器是一个高度面向对象的代码库，对象之间互相引用，到处是共享可变状态，依赖垃圾回收。这种代码搬到 Go 里，很多地方可以近乎一比一地翻译。搬到 Rust 里，你得先想清楚每个对象的所有权归谁、生命周期多长——那不叫移植，那叫重新设计。</p> <p>而 TypeScript 恰恰不是一份能重新设计的东西。这个编译器里堆了大约 100 人年的工作量，绝大部分不是算法，是十几年攒下来的兼容性判断：这种写法在 2014 年的某个版本里被允许过，有人依赖它，所以现在还得允许；那种推断在某个边界条件下会退化成 any，改了会炸掉一批库。这些东西没有文档，只活在实现里。</p> <p>要把这种东西搬到另一门语言，最重要的性能指标不是运行时速度，是「一比一搬过去的难度」。Go 在这个指标上赢了 Rust。</p> <p><img src="https://bougieL.github.io/images/2026/09/16/fig-2.jpeg" alt="两块路牌指向两个不同的方向" loading="lazy"></p> <h2 id="移植和重写是两件完全不同的事"> 移植和重写是两件完全不同的事</h2> <p>我想把这条线单独拎出来，因为它解释了不少看起来矛盾的现象。</p> <p>esbuild、Rolldown、Oxc 是从零开始设计的。目标很明确：吃掉 JS/TS 的一个子集，在这个子集上做到最快。语法支持不完整可以慢慢补，边界情况可以先不管。它们有资格选 Rust，因为没有历史包袱需要一比一保留。</p> <p>TypeScript 没有这个资格。它的全部价值就在那些边界情况里。一个「支持 95% 语法、快 50 倍」的 TypeScript 是没有意义的，因为那 5% 里全是别人的生产代码。</p> <p>所以它只能选一门能让它把包袱一起带走的语言，哪怕那门语言理论上没那么快。</p> <p>选语言这件事，从来不是选语言本身，是选你能带走多少过去。</p> <p><img src="https://bougieL.github.io/images/2026/09/16/fig-3.jpeg" alt="一摞图纸从一张桌子搬到旁边另一张桌子，一张都没有重画" loading="lazy"></p> <h2 id="慢工具会替你做决定"> 慢工具会替你做决定</h2> <p>说完实现，说点跟日常更有关的。</p> <p>我一直觉得工具的性能被低估了，因为它省下的不只是时间，它还会改变你工作的形状。</p> <p>类型检查要跑 60 秒的时候，你不会把它挂在保存时自动执行。你会把它挪到 CI，挪到 pre-push，最后挪到「上线前想起来就跑一下」。lint 慢的时候你会关掉几条规则，因为「这条规则不太值」。构建慢的时候你会倾向于把改动攒成一堆再统一验证，而不是改一点验一点。</p> <p>这些决定每一个单独看都合理。加在一起，它们把你的节奏往「大批量、低频次、延迟反馈」的方向推。你以为这是你的工作习惯，其实有一部分是你工具的延迟在替你决定。</p> <p>当类型检查从 60 秒变成 5 秒，发生的不只是省了 55 秒。它被挂回保存时自动执行了，错误在你写完下一行之前就冒出来，你开始敢在重构的时候大范围改类型。这些都是慢工具时代根本不会进入选项的做法。</p> <h2 id="我并不确定这全是好事"> 我并不确定这全是好事</h2> <p>写到这儿该泼点冷水。</p> <p>这一轮重写有个很少被提的代价：前端工程师对自己工具链的可读性在下降。</p> <p>十年前你用的工具基本都是 JS 写的。Babel 插件你会写，webpack 配置你读得懂，遇到诡异行为能 debugger 进源码去看。现在你项目里跑的东西，大多是 Rust 或 Go 写的二进制。它对你来说是个黑盒——快，但你打不开。</p> <p><img src="https://bougieL.github.io/images/2026/09/16/fig-4.jpeg" alt="一个人站在打开的机器外壳前，里面是他看不懂的结构" loading="lazy"></p> <p>这不一定坏。我不认为每个用编译器的人都需要读懂编译器，我读不懂 V8，照样写 JS。但这里有个区别：V8 从来就是黑盒，我们从来没有拥有过它；而 Babel 和 webpack 是我们自己的东西，是我们生态的一部分，是用我们的语言写的。这个所有感没了，是有点可惜的。</p> <p>还有个更实际的问题：谁来维护。旧的 JS 工具靠社区，新的原生工具靠公司。公司会撤资，会调整战略，会把团队挪去别的项目。Oxc、Rolldown、Rspack 现在状态都不错，但把整个生态的基础设施压在少数几家公司的预算上，风险不在今天，在五年后。</p> <h2 id="结尾"> 结尾</h2> <p>我不怀念等 webpack 编译的那两分钟。真的，一点都不怀念。</p> <p>但我确实有点怀念那种「这个项目我现在看不懂，但我可以花一个下午看懂它」的感觉。以前遇到工具的诡异行为，我的默认动作是打开 <code>node_modules</code> 翻源码，而且经常能翻到答案。现在我的默认动作是去提 issue。</p> <p>工具链变快了，也变远了。</p> <p>这大概就是进步的常态——你拿到一样东西，同时交出另一样。只是这次交出去的是「随时能看懂」，而它值不值那个 47 秒到 6 秒的差价，取决于你接下来这几年打算怎么用它。</p> <hr> <p>题图与部分配图来自开放授权的摄影作品，感谢这些作者：Tool rack — 1lenore；Stopwatch test — casey.marshall；Crossroads: Invest or Spend — ccPixs.com；Gears gears cogs bits n pieces — Elsie esq.（均为 CC BY 2.0，Flickr）。</p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[CSS 悄悄补齐的那些短板]]></title>
            <description><![CDATA[翻一个 2021 年的老项目，看到一段用 ResizeObserver 手写响应式卡片的代码，我盯着它看了很久——现在这件事是四行 CSS。容器查询、:has()、原生嵌套、@layer、subgrid、oklch，这些年 CSS 补上的东西比大多数人的印象要多。]]></description>
            <link>https://bougieL.github.io/2026/09/15/css-caught-up-quietly/</link>
            <guid isPermaLink="true">https://bougieL.github.io/2026/09/15/css-caught-up-quietly/</guid>
            <pubDate>Tue, 15 Sep 2026 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<p>前阵子回去改一个 2021 年的老项目，翻到一段让我停下来的代码。</p> <p>它是一个卡片列表的「响应式」实现。需求很普通：卡片放在主内容区的时候横向排列，侧边栏收起变宽时就紧凑一点，塞进右侧栏的时候就竖过来。当年写这段代码的人（大概率是我）用了一个 <code>ResizeObserver</code> 监听容器，然后手动往卡片上加 <code>data-compact</code> 属性，再配三条 CSS 规则。</p> <p>四十多行 JS，加一堆属性，加一个防抖。我盯着它看了半天，心情有点复杂。</p> <p>现在这件事长这样：</p> <div class="language-css extra-class"><pre class="language-css"><code><span class="token selector">.card-list</span> <span class="token punctuation">{</span>
  <span class="token property">container-type</span><span class="token punctuation">:</span> inline-size<span class="token punctuation">;</span>
<span class="token punctuation">}</span>

<span class="token atrule"><span class="token rule">@container</span> <span class="token punctuation">(</span><span class="token property">min-width</span><span class="token punctuation">:</span> 480px<span class="token punctuation">)</span></span> <span class="token punctuation">{</span>
  <span class="token selector">.card</span> <span class="token punctuation">{</span>
    <span class="token property">display</span><span class="token punctuation">:</span> grid<span class="token punctuation">;</span>
    <span class="token property">grid-template-columns</span><span class="token punctuation">:</span> 120px 1fr<span class="token punctuation">;</span>
  <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre></div><p>四行。没有 JS，没有观察器，没有防抖，不需要记得在组件卸载时 <code>disconnect()</code>。</p> <p>代码变得这么短不是重点。重点是：我写那段 <code>ResizeObserver</code> 的时候，完全没觉得哪里不对。它甚至算得上当时社区推荐的做法。我们这一代前端对 CSS 的默认期待被压得很低，低到「样式语言不该知道自己的容器有多大」这种事，我们很自然地接受了，然后自己用 JS 补上。</p> <p>而 CSS 在过去几年里，把这一类「我们自己补上的东西」一件件收了回去。</p> <p><img src="https://bougieL.github.io/images/2026/09/15/fig-1.jpeg" alt="同一个卡片组件在三种容器宽度下的形态" loading="lazy"></p> <h2 id="媒体查询问错了问题"> 媒体查询问错了问题</h2> <p>媒体查询查的是视口。这在 2010 年是够用的，因为那时候页面基本就是一整列从上往下流，视口宽度几乎是布局的唯一变量。</p> <p>后来组件化来了，同一个卡片可能出现在页面主区、右侧栏、弹窗、手机端的抽屉里。它该长什么样，跟视口一点关系都没有，只跟它自己被塞进的那个格子有多大有关。媒体查询回答不了一个它没被问的问题。</p> <p>容器查询把这个关系摆正了：</p> <div class="language-css extra-class"><pre class="language-css"><code><span class="token selector">.sidebar</span> <span class="token punctuation">{</span>
  <span class="token property">container-type</span><span class="token punctuation">:</span> inline-size<span class="token punctuation">;</span>
  <span class="token property">container-name</span><span class="token punctuation">:</span> sidebar<span class="token punctuation">;</span>
<span class="token punctuation">}</span>

<span class="token atrule"><span class="token rule">@container</span> sidebar <span class="token punctuation">(</span><span class="token property">max-width</span><span class="token punctuation">:</span> 320px<span class="token punctuation">)</span></span> <span class="token punctuation">{</span>
  <span class="token selector">.card__meta</span> <span class="token punctuation">{</span>
    <span class="token property">display</span><span class="token punctuation">:</span> none<span class="token punctuation">;</span>
  <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre></div><p><code>container-type: inline-size</code> 这句有个副作用容易被忽略——它给元素加了一层「尺寸限制」，元素的行内尺寸不再由内容撑开，而是由父级决定。第一次用的人经常在这里卡住：加了容器查询之后布局塌了。这不是 bug，是必须付的代价，浏览器得先知道容器多大，才能拿它去查。</p> <p><code>container-name</code> 是可选的，但在一个页面里有好几层容器的时候，我建议写上。不写的话查询会去找最近的祖先容器，嵌套一深，读代码的人就得在 DOM 树里来回跳。</p> <p>还有 <code>cqi</code>、<code>cqw</code> 这些单位，可以做随容器缩放的字号：</p> <div class="language-css extra-class"><pre class="language-css"><code><span class="token selector">.card__title</span> <span class="token punctuation">{</span>
  <span class="token property">font-size</span><span class="token punctuation">:</span> <span class="token function">clamp</span><span class="token punctuation">(</span>1rem<span class="token punctuation">,</span> 4cqi<span class="token punctuation">,</span> 1.5rem<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre></div><p>一行代码，标题跟着容器长大。放在以前这是 JS 的活儿，或者是一个妥协掉的固定字号。</p> <p><img src="https://bougieL.github.io/images/2026/09/15/fig-2.jpeg" alt="一个父元素因为内部某个小方块而整体改变形态" loading="lazy"></p> <h2 id="has-把-css-的单向箭头掰了回来"> <code>:has()</code> 把 CSS 的单向箭头掰了回来</h2> <p>CSS 的选择器一直是单向的：你能选中后代，不能因为后代去选中祖先。这是有性能上的理由的，反向匹配代价太高，多年来说起父选择器，社区的标准回答是「不可能」。</p> <p>然后它就这么来了：</p> <div class="language-css extra-class"><pre class="language-css"><code><span class="token selector">.card:has(img)</span> <span class="token punctuation">{</span>
  <span class="token property">padding-top</span><span class="token punctuation">:</span> 0<span class="token punctuation">;</span>
<span class="token punctuation">}</span>

<span class="token selector">.card:not(:has(img))</span> <span class="token punctuation">{</span>
  <span class="token property">padding-top</span><span class="token punctuation">:</span> 1.25rem<span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre></div><p>看起来只是省了一个 <code>has-cover</code> 的 class。但真正的变化是，判断依据从「模板里写没写这个 class」变成了「渲染出来到底有没有这个元素」。这个区别在一个由数据驱动、字段可能为空的列表里很实在——以前图片字段为空时，卡片顶部会留一块空白，因为模板里的 class 是写死的。</p> <p>表单大概是 <code>:has()</code> 最舒服的用武之地：</p> <div class="language-css extra-class"><pre class="language-css"><code><span class="token selector">.field:has(input:invalid:not(:placeholder-shown))</span> <span class="token punctuation">{</span>
  <span class="token property">border-color</span><span class="token punctuation">:</span> #d64545<span class="token punctuation">;</span>
<span class="token punctuation">}</span>

<span class="token selector">.field:has(input:valid)</span> <span class="token punctuation">{</span>
  <span class="token property">border-color</span><span class="token punctuation">:</span> #2f9e6f<span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre></div><p><code>:invalid</code> 早就有了，麻烦在于它一上来就是 invalid——用户还没输入，输入框就红了。<code>:placeholder-shown</code> 是个老技巧，用来判断「还没填过」。以前这个判断只能落在 input 上，样式也只能改 input 自己；现在可以往上抬到整个 <code>.field</code>，label 的提示文字、边框、图标一起变。</p> <p>我还见过一个很妙的用法，是拿它做全局状态：</p> <div class="language-css extra-class"><pre class="language-css"><code><span class="token selector">body:has(.modal[open])</span> <span class="token punctuation">{</span>
  <span class="token property">overflow</span><span class="token punctuation">:</span> hidden<span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre></div><p>不用再在打开弹窗的 JS 里记得加个 <code>body.classList.add('no-scroll')</code>，也就不会再出现「关掉弹窗忘记移除、页面滚不动了」这种经典 bug。</p> <h2 id="嵌套终于不用装插件了"> 嵌套终于不用装插件了</h2> <p>我用 Sass 很多年，嵌套是当初离不开它的头号理由。为了这个语法，我们接受了一整套构建流程、一个编译步骤、以及「改完保存等两秒」。</p> <p>现在它是原生的：</p> <div class="language-css extra-class"><pre class="language-css"><code><span class="token selector">.card</span> <span class="token punctuation">{</span>
  <span class="token property">padding</span><span class="token punctuation">:</span> 1rem<span class="token punctuation">;</span>

  <span class="token selector">&amp; h3</span> <span class="token punctuation">{</span>
    <span class="token property">font-size</span><span class="token punctuation">:</span> 1.1rem<span class="token punctuation">;</span>
  <span class="token punctuation">}</span>

  <span class="token selector">&amp;:hover</span> <span class="token punctuation">{</span>
    <span class="token property">border-color</span><span class="token punctuation">:</span> <span class="token function">oklch</span><span class="token punctuation">(</span>70% 0.15 250<span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token punctuation">}</span>

  <span class="token atrule"><span class="token rule">@container</span> <span class="token punctuation">(</span><span class="token property">min-width</span><span class="token punctuation">:</span> 480px<span class="token punctuation">)</span></span> <span class="token punctuation">{</span>
    <span class="token property">display</span><span class="token punctuation">:</span> grid<span class="token punctuation">;</span>
  <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre></div><p>有个坑值得单独说，因为它坑过我一次：在原生嵌套里，<code>&amp;</code> 不是可选的。</p> <div class="language-css extra-class"><pre class="language-css"><code><span class="token selector">.card</span> <span class="token punctuation">{</span>
  <span class="token selector">.title</span> <span class="token punctuation">{</span> <span class="token punctuation">}</span>   <span class="token comment">/* 不会报错，但这个选择器是 .card .title 吗？不是 */</span>
<span class="token punctuation">}</span>
</code></pre></div><p>上面那行在 Sass 里是后代选择器，在原生 CSS 里，<code>&amp;</code> 会被隐式加在<strong>整个</strong>选择器前面 —— 但只有对 <code>&amp;</code> 的隐式插入有明确定义的写法才是安全的。写 <code>.title</code> 这种裸类选择器，浏览器会把它解析成 <code>is(.card) .title</code> 吗？实际上规范的行为是隐式 <code>&amp;</code> 只在能明确插入时生效，而裸元素选择器 <code>h3</code> 会产生歧义（<code>h3</code> 到底是后代还是什么），所以<strong>元素选择器必须写成 <code>&amp; h3</code> 或者 <code>:is(h3)</code></strong>。</p> <p>所以规矩很简单：<strong>嵌套里写标签选择器，前面一定要带 <code>&amp;</code></strong>。类选择器可以省，但带上是更省心的习惯。</p> <p>另一点是原生嵌套没有 Sass 的 <code>&amp;__title</code> 拼接。<code>&amp;</code> 是一个完整的选择器，不是字符串。想在原生 CSS 里拼 BEM 式的类名，得老实写全。这个限制我一开始嫌烦，后来觉得是好事——<code>&amp;__title</code> 这种写法搜不到、跳转不了、重构的时候最容易漏。</p> <p><img src="https://bougieL.github.io/images/2026/09/15/fig-3.jpeg" alt="层层叠放的半透明图层，表示级联顺序" loading="lazy"></p> <h2 id="layer-治的是一种慢性病"> <code>@layer</code> 治的是一种慢性病</h2> <p>每个项目到了一定规模都会得同一种病：某个组件的样式不生效，于是有人加了 <code>!important</code>，再后来有人加了更具体的选择器，最后大家都不敢删任何一行 CSS。</p> <p><code>@layer</code> 给的不是一个新技巧，是一个可以提前声明的秩序：</p> <div class="language-css extra-class"><pre class="language-css"><code><span class="token atrule"><span class="token rule">@layer</span> reset<span class="token punctuation">,</span> base<span class="token punctuation">,</span> components<span class="token punctuation">,</span> utilities<span class="token punctuation">;</span></span>

<span class="token atrule"><span class="token rule">@layer</span> reset</span> <span class="token punctuation">{</span>
  <span class="token selector">*</span> <span class="token punctuation">{</span> <span class="token property">margin</span><span class="token punctuation">:</span> 0<span class="token punctuation">;</span> <span class="token property">box-sizing</span><span class="token punctuation">:</span> border-box<span class="token punctuation">;</span> <span class="token punctuation">}</span>
<span class="token punctuation">}</span>

<span class="token atrule"><span class="token rule">@layer</span> utilities</span> <span class="token punctuation">{</span>
  <span class="token selector">.hidden</span> <span class="token punctuation">{</span> <span class="token property">display</span><span class="token punctuation">:</span> none<span class="token punctuation">;</span> <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre></div><p>关键点在于：<strong>层的优先级高于选择器特异性</strong>。<code>utilities</code> 里的 <code>.hidden</code> 会赢过 <code>components</code> 里写得再具体的 <code>.modal .header .close .hidden</code>，不需要 <code>!important</code>，不需要比谁的选择器更长。</p> <p>这意味着「后写的能覆盖先写的」重新变成了一条可以依赖的规则。对一个接手别人代码的人来说，这条规则值很多钱。</p> <p>我们团队现在做新项目，第一件事就是把这几层声明写在入口文件最上面。它不解决所有问题，但它把一类问题的发生概率从「迟早会」降到了「一般不会」。</p> <h2 id="subgrid-补的是最后一块拼图"> subgrid 补的是最后一块拼图</h2> <p>Grid 出来之后有个很尴尬的场景：一排卡片高度不一样，你想让它们的标题、正文、按钮各自水平对齐。用 Grid 排外层的卡片没问题，但每张卡片内部是独立的 Grid 上下文，它们之间对不上。</p> <p>以前的办法是给卡片固定高度，或者用 flex 加 <code>margin-top: auto</code> 把按钮推到底部——能看，但标题还是不齐。</p> <div class="language-css extra-class"><pre class="language-css"><code><span class="token selector">.list</span> <span class="token punctuation">{</span>
  <span class="token property">display</span><span class="token punctuation">:</span> grid<span class="token punctuation">;</span>
  <span class="token property">grid-template-columns</span><span class="token punctuation">:</span> <span class="token function">repeat</span><span class="token punctuation">(</span>auto-fill<span class="token punctuation">,</span> <span class="token function">minmax</span><span class="token punctuation">(</span>240px<span class="token punctuation">,</span> 1fr<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token property">grid-template-rows</span><span class="token punctuation">:</span> auto auto auto<span class="token punctuation">;</span>
<span class="token punctuation">}</span>

<span class="token selector">.card</span> <span class="token punctuation">{</span>
  <span class="token property">display</span><span class="token punctuation">:</span> grid<span class="token punctuation">;</span>
  <span class="token property">grid-row</span><span class="token punctuation">:</span> span 3<span class="token punctuation">;</span>
  <span class="token property">grid-template-rows</span><span class="token punctuation">:</span> subgrid<span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre></div><p>卡片声明 <code>grid-template-rows: subgrid</code>，意思是不自己另起一套行，而是直接用父级的那三行。于是同一横排的卡片，标题对齐标题，按钮对齐按钮。</p> <p>Firefox 2019 年就支持了，Chrome 拖到 2023 年 9 月才跟上，中间这四年 subgrid 基本处于「知道有，但线上不敢用」的状态。现在它可以放心用。</p> <p><img src="https://bougieL.github.io/images/2026/09/15/fig-4.jpeg" alt="一条平滑穿过感知均匀色彩空间的色带" loading="lazy"></p> <h2 id="颜色这部分-是肉眼能看出来的差别"> 颜色这部分，是肉眼能看出来的差别</h2> <p><code>oklch()</code> 值得单独拿出来说，因为它解决的不是写法问题，是效果问题。</p> <div class="language-css extra-class"><pre class="language-css"><code><span class="token selector">:root</span> <span class="token punctuation">{</span>
  <span class="token property">--brand</span><span class="token punctuation">:</span> <span class="token function">oklch</span><span class="token punctuation">(</span>62% 0.17 252<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>

<span class="token selector">.btn:hover</span> <span class="token punctuation">{</span>
  <span class="token property">background</span><span class="token punctuation">:</span> <span class="token function">oklch</span><span class="token punctuation">(</span>from <span class="token function">var</span><span class="token punctuation">(</span>--brand<span class="token punctuation">)</span> <span class="token function">calc</span><span class="token punctuation">(</span>l + 0.08<span class="token punctuation">)</span> c h<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre></div><p><code>oklch</code> 的三个分量是亮度、彩度、色相，而且它是对人眼感知均匀的。同样把亮度加 10%，在 <code>hsl</code> 里黄色会明显变灰、蓝色几乎没变化，在 <code>oklch</code> 里两者的观感变化是一致的。做设计系统的人应该对这个痛点很熟：定了一组主色，想生成 hover、active、disabled 的深浅变体，用 <code>hsl</code> 调出来的东西总有几个颜色不对劲，得手动微调。</p> <p><code>oklch(from ...)</code> 这种相对颜色语法把「基于一个已有颜色派生」变成了语言里的原生能力。以前要么靠 Sass 函数，要么靠 CSS 变量把 L、C、H 拆成三个变量存。现在一行搞定。</p> <p><code>color-mix()</code> 也很好用：</p> <div class="language-css extra-class"><pre class="language-css"><code><span class="token selector">.tag</span> <span class="token punctuation">{</span>
  <span class="token property">background</span><span class="token punctuation">:</span> <span class="token function">color-mix</span><span class="token punctuation">(</span>in oklch<span class="token punctuation">,</span> <span class="token function">var</span><span class="token punctuation">(</span>--brand<span class="token punctuation">)</span> 15%<span class="token punctuation">,</span> white<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre></div><p>我上一次做主题切换的时候，靠这两个函数把原来二十多个写死的色值压到了六个基础色。规模小了，出错的机会也就少了。</p> <p>顺便提一句 <code>light-dark()</code>，配合 <code>color-scheme</code> 可以不写媒体查询就跟随系统主题：</p> <div class="language-css extra-class"><pre class="language-css"><code><span class="token selector">:root</span> <span class="token punctuation">{</span> <span class="token property">color-scheme</span><span class="token punctuation">:</span> light dark<span class="token punctuation">;</span> <span class="token punctuation">}</span>

<span class="token selector">body</span> <span class="token punctuation">{</span>
  <span class="token property">background</span><span class="token punctuation">:</span> <span class="token function">light-dark</span><span class="token punctuation">(</span>#ffffff<span class="token punctuation">,</span> #14161a<span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token property">color</span><span class="token punctuation">:</span> <span class="token function">light-dark</span><span class="token punctuation">(</span>#1a1a1a<span class="token punctuation">,</span> #e8e8e8<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre></div><h2 id="还有一批在路上的"> 还有一批在路上的</h2> <p>有些东西现在还只能算「Chrome 先行」，生产环境要谨慎，但值得知道：</p> <ul><li><strong>视图过渡</strong>（View Transitions）：<code>document.startViewTransition()</code> 一行就能让页面切换有过渡动画，跨文档版本也支持了。Safari 和 Firefox 的进度不一样，得看目标用户的浏览器分布。</li> <li><strong>滚动驱动动画</strong>（scroll-driven animations）：<code>animation-timeline: scroll()</code>，滚动条变成动画的时间轴。以前这类效果基本要上 GSAP 或手写监听。</li> <li><strong>锚点定位</strong>（anchor positioning）：把浮层定位到某个元素旁边，纯 CSS，不用再算 <code>getBoundingClientRect</code>。</li></ul> <p>这三个我都只在内部工具里用过，因为它们的降级体验不好做。倒是 <code>text-wrap: balance</code> 可以随便用，它只影响排版，不支持的浏览器原样显示，一行代码让标题换行更均匀：</p> <div class="language-css extra-class"><pre class="language-css"><code><span class="token selector">h1, h2</span> <span class="token punctuation">{</span>
  <span class="token property">text-wrap</span><span class="token punctuation">:</span> balance<span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre></div><h2 id="我们卡住的其实是肌肉记忆"> 我们卡住的其实是肌肉记忆</h2> <p>写这篇文章的时候我一直在想一个问题：这些特性里，容器查询 2023 年初就全平台可用了，<code>:has()</code> 到 2023 年底也齐了，到今天已经过去快三年。可我在代码评审里，还是经常看到用 JS 实现这些效果的新代码。</p> <p>不是大家不知道，是肌肉记忆还在。我们脑子里有一张「哪些事得用 JS 做」的清单，这张清单是 2018 年前后形成的，之后就没怎么更新过。遇到响应式卡片，手先伸向 ResizeObserver；遇到父元素要变样式，手先伸向 classList。这些动作不需要思考，所以也不会触发「等一下，CSS 现在是不是能做了」这个念头。</p> <p>另一半原因是 CSS 这十年的名声不太好。它被当成一门「靠试」的语言，写对了不知道为什么对，写错了不知道为什么错。于是大家倾向于用自己能推理的语言去解决布局问题，哪怕绕远路。这个倾向是有道理的，但代价是我们背上了一堆本不该存在的 JS。</p> <p>我现在的习惯是，动手写布局之前先花两分钟去 MDN 上确认一下「这件事现在 CSS 能不能做」。这个两分钟经常能省掉几十行代码，和它们后续所有的维护成本。</p> <p>CSS 已经不是我们认识的那个 CSS 了。麻烦的是，我们还是那批写 CSS 的人。</p>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[代码写了十年，我给自己报了个生活补课班]]></title>
            <description><![CDATA[一台轰隆作响的洗衣机、半锅炒糊的糖色、一把拧秃了的螺丝刀——程序员的「代码思维」在生活里哪些好用、哪些失灵，我是从这个周末开始认真想的。]]></description>
            <link>https://bougieL.github.io/2026/09/14/life-skills-beyond-code/</link>
            <guid isPermaLink="true">https://bougieL.github.io/2026/09/14/life-skills-beyond-code/</guid>
            <pubDate>Mon, 14 Sep 2026 00:00:00 GMT</pubDate>
            <content:encoded><![CDATA[<p>上上个周末，我家洗衣机突然开始发出一种像是有人在里面摇骰子的声音。</p> <p>要搁以前，我的第一反应是打开购物软件，直接换一台新的。但那天不知道哪根筋搭错了，我蹲下来看了看它，心想：不就是异响吗，我又不是没排查过线上故障。</p> <p>于是那个周末，我人生第一次拆开了洗衣机。</p> <p><img src="https://bougieL.github.io/images/2026/09/14/fig-1.jpeg" alt="人生第一次拆洗衣机：排查很顺利，拧螺丝很狼狈" loading="lazy"></p> <h2 id="排查问题我会-拧螺丝我是真的菜"> 排查问题我会，拧螺丝我是真的菜</h2> <p>故事的结局先说：堵住排水泵的，是一枚发卡。外加两枚硬币和一团不明黑色织物。</p> <p>排查过程其实很顺利。异响只在排水的时候出现，那大概率是排水泵；排水泵在洗衣机前下方，拆开面板就能看到。这套「根据现象缩小范围」的思路，跟我在生产环境里追一个偶发 bug 没有任何区别——看日志、二分、定位、验证。我甚至下意识地在心里给每一步打了个「假设：排水泵有异物」的标记。</p> <p>但一上手，我就露馅了。</p> <p>面板上有四颗螺丝。我找了一圈家里的工具，只找到一把随某个家电附赠的、型号不知道匹不匹配的螺丝刀。拧第一颗的时候还行，拧第二颗的时候滑了一次丝，拧到第三颗，我的手心已经开始出汗。四颗螺丝，我拧了十分钟。一个在代码里把「一切皆可自动化」当信仰的人，被四颗物理螺丝按在地板上摩擦。</p> <p>那一刻我意识到一件事：我排查问题的能力是十年工作练出来的，但我动手的能力，可能还停留在普通用户的水平。我的工具箱，比我的 IDE 寒酸多了。</p> <p><img src="https://bougieL.github.io/images/2026/09/14/fig-2.jpeg" alt="我的工具箱，和 IDE 摆在一起确实有点寒酸" loading="lazy"></p> <h2 id="生活里没有-npm-install"> 生活里没有 npm install</h2> <p>后来我开始留意这种落差，发现不止是修东西。</p> <p>比如做饭。我做饭的水平长期停留在「能熟」这一档，因为在我原来的世界观里，做饭是个效率问题：填饱肚子，消耗最少的注意力和时间。外卖出现之后，这个问题被彻底外包了。</p> <p>直到有一次朋友要来家里吃饭，我决定认真做一次红烧肉。我照着菜谱一步步来。菜谱写得很清楚，像一份文档：焯水、炒糖色、小火四十分钟。但我很快就发现了菜谱这份「文档」和真正的技术文档的区别——它不告诉你参数的边界条件。「小火」是多小的火？我家灶拧到第几格才算小火？「炒糖色到枣红色」，枣红色和炒糊之间隔几秒？</p> <p>菜谱默认你有一些我没学过的先验知识。就像一份假设你读过整个项目历史的代码，注释写得再好，落到没上过手的人身上，还是步步惊心。那天那锅红烧肉，糖色糊了半锅，最后靠加水和糖硬救回来。朋友很给面子地说好吃，我自己心里清楚，那是幸存者偏差。</p> <p><img src="https://bougieL.github.io/images/2026/09/14/fig-3.jpeg" alt="灶台上小火慢炖的那锅红烧肉，糖色炒糊过一次" loading="lazy"></p> <h2 id="代码思维能迁移一半"> 代码思维能迁移一半</h2> <p>有意思的是，也不是所有东西都要从零学。那个周末之后我大概盘了一下，程序员的职业病里，有一部分在生活里其实特别好使：</p> <ul><li><strong>排查思维</strong>。异响、漏水、灯不亮，任何「现象」都可以当成日志来读。先缩小范围再动手，能省一半力气。</li> <li><strong>读手册的习惯</strong>。我以前修东西全靠瞎拧，现在会先花十分钟把说明书翻到故障排查那一页。大部分家电的说明书里真的写了，就是没人看。</li> <li><strong>「先备份再动手」</strong>。拆之前拍照，记下每颗螺丝的位置。这条习惯救了我至少两次。</li></ul> <p>但另一部分，是真迁移不过去的。</p> <p>比如「手感」。螺丝拧到什么程度算紧而不滑丝，糖色炒到什么颜色算刚好，面揉到什么程度算醒好了——这些东西没有报错信息，没有类型标注，没有一个 CI 会跑过来说你失败了。它们只存在于你的手上，只能靠一次次做错来校准。</p> <p>我把这类东西叫做「具身知识」。写代码十年，我攒了一脑袋的抽象知识，但具身知识几乎是零。而生活恰恰是由具身知识撑着的：火候、手感、分寸。这些东西买不来，搜索不来，AI 也替你练不了。</p> <h2 id="修好那一刻-跟-debug-通了是一样的"> 修好那一刻，跟 debug 通了是一样的</h2> <p>洗衣机修好之后，我把它推回原位，插上电，按下启动键。滚筒转起来，安安静静。那枚发卡现在躺在我的工具箱里当纪念品。</p> <p>那个瞬间的爽感，我非常熟悉——就是 debug 到凌晨，突然发现那个 off-by-one 的瞬间。现象消失，假设验证，世界恢复秩序。一模一样的多巴胺，而且不用写复盘，不用排期，没有 oncall。</p> <p>我不觉得这是什么「程序员必须学生活技能」的大道理。我只是发现，以前我把生活里的问题都当成了干扰项：漏水了找人修，吃饭点外卖，坏了就换新。我以为这叫效率，其实是我把自己的生活一点点外包了出去，外包到我自己的手越来越笨。</p> <p>现在我给自己定了个小规矩：生活里坏掉的小东西，先试着修，修不好再买。上个月修好了洗衣机，这个月换掉了卫生间的密封条，下周打算挑战那把从搬进来就晃悠的椅子。</p> <p><img src="https://bougieL.github.io/images/2026/09/14/fig-4.jpeg" alt="傍晚窗台上的一杯茶，世界恢复秩序" loading="lazy"></p> <p>代码写了十年，是时候补一补生活这门课了。学费是四颗螺丝、半锅糖色，和一把拧秃了的螺丝刀。不贵。</p>]]></content:encoded>
        </item>
    </channel>
</rss>